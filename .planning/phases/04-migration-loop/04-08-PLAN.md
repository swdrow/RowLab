---
phase: 04-migration-loop
plan: 08
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - src/v2/components/fleet/ShellsTable.tsx
  - src/v2/components/fleet/OarsTable.tsx
  - src/v2/components/fleet/ShellForm.tsx
  - src/v2/components/fleet/OarSetForm.tsx
  - src/v2/components/common/CrudModal.tsx
  - src/v2/components/fleet/index.ts
  - src/v2/components/common/index.ts
autonomous: true

must_haves:
  truths:
    - "ShellsTable displays list with name, class, status columns"
    - "OarsTable displays list with name, type, count, status columns"
    - "ShellForm uses react-hook-form with Zod validation"
    - "OarSetForm uses react-hook-form with Zod validation"
    - "CrudModal provides reusable modal wrapper with title and close button"
    - "Tables have edit and delete actions for coaches"
  artifacts:
    - path: "src/v2/components/fleet/ShellsTable.tsx"
      provides: "Shells list table"
      exports: ["ShellsTable"]
    - path: "src/v2/components/fleet/OarsTable.tsx"
      provides: "Oar sets list table"
      exports: ["OarsTable"]
    - path: "src/v2/components/fleet/ShellForm.tsx"
      provides: "Shell create/edit form"
      exports: ["ShellForm"]
    - path: "src/v2/components/fleet/OarSetForm.tsx"
      provides: "OarSet create/edit form"
      exports: ["OarSetForm"]
    - path: "src/v2/components/common/CrudModal.tsx"
      provides: "Reusable modal wrapper"
      exports: ["CrudModal"]
  key_links:
    - from: "src/v2/components/fleet/ShellForm.tsx"
      to: "react-hook-form"
      via: "useForm import"
      pattern: "import.*useForm.*from 'react-hook-form'"
    - from: "src/v2/components/fleet/ShellForm.tsx"
      to: "@hookform/resolvers/zod"
      via: "zodResolver import"
      pattern: "import.*zodResolver"
---

<objective>
Create fleet management UI components: tables for shells and oars display, forms with react-hook-form + Zod validation, and reusable modal wrapper.

Purpose: Equipment inventory management for coaches (COACH-03, COACH-04).
Output: Table and form components for shells and oar sets with proper validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-migration-loop/04-RESEARCH.md (Pattern 2: react-hook-form + Zod)
@src/v2/types/coach.ts (types from 04-05)
@src/v2/hooks/useShells.ts (from 04-05)
@src/v2/hooks/useOarSets.ts (from 04-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CrudModal component</name>
  <files>src/v2/components/common/CrudModal.tsx, src/v2/components/common/index.ts</files>
  <action>
Create src/v2/components/common/CrudModal.tsx as reusable modal wrapper:

```typescript
import { Fragment } from 'react';
import { Dialog, Transition } from '@headlessui/react';
import { X } from 'lucide-react';

interface CrudModalProps {
  isOpen: boolean;
  onClose: () => void;
  title: string;
  children: React.ReactNode;
}

export function CrudModal({ isOpen, onClose, title, children }: CrudModalProps) {
  return (
    <Transition appear show={isOpen} as={Fragment}>
      <Dialog as="div" className="relative z-50" onClose={onClose}>
        {/* Backdrop */}
        <Transition.Child
          as={Fragment}
          enter="ease-out duration-200"
          enterFrom="opacity-0"
          enterTo="opacity-100"
          leave="ease-in duration-150"
          leaveFrom="opacity-100"
          leaveTo="opacity-0"
        >
          <div className="fixed inset-0 bg-black/50 backdrop-blur-sm" />
        </Transition.Child>

        {/* Modal panel */}
        <div className="fixed inset-0 overflow-y-auto">
          <div className="flex min-h-full items-center justify-center p-4">
            <Transition.Child
              as={Fragment}
              enter="ease-out duration-200"
              enterFrom="opacity-0 scale-95"
              enterTo="opacity-100 scale-100"
              leave="ease-in duration-150"
              leaveFrom="opacity-100 scale-100"
              leaveTo="opacity-0 scale-95"
            >
              <Dialog.Panel className="w-full max-w-lg transform overflow-hidden rounded-xl bg-card-bg border border-bdr-primary p-6 text-left align-middle shadow-xl transition-all">
                {/* Header */}
                <div className="flex items-center justify-between mb-4">
                  <Dialog.Title className="text-lg font-semibold text-txt-primary">
                    {title}
                  </Dialog.Title>
                  <button
                    onClick={onClose}
                    className="rounded-lg p-1.5 hover:bg-surface-hover transition-colors"
                  >
                    <X size={20} className="text-txt-secondary" />
                  </button>
                </div>

                {/* Content */}
                {children}
              </Dialog.Panel>
            </Transition.Child>
          </div>
        </div>
      </Dialog>
    </Transition>
  );
}
```

Check if @headlessui/react is installed. If not, add the import pattern that works with existing project setup (may need to install: `npm install @headlessui/react`).

Create src/v2/components/common/index.ts:
```typescript
export { CrudModal } from './CrudModal';
```
  </action>
  <verify>
Files exist at src/v2/components/common/CrudModal.tsx and index.ts.
  </verify>
  <done>
CrudModal provides reusable modal wrapper with V2 styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ShellForm with react-hook-form</name>
  <files>src/v2/components/fleet/ShellForm.tsx</files>
  <action>
Create src/v2/components/fleet/ShellForm.tsx following RESEARCH.md Pattern 2:

```typescript
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import type { Shell } from '../../types/coach';

const shellSchema = z.object({
  name: z.string().min(1, 'Name is required').max(100),
  boatClass: z.string().min(1, 'Boat class is required'),
  type: z.enum(['RACING', 'TRAINING', 'RECREATIONAL']),
  weightClass: z.enum(['LIGHTWEIGHT', 'HEAVYWEIGHT', 'OPEN']),
  rigging: z.enum(['STARBOARD', 'PORT', 'BUCKET']),
  status: z.enum(['AVAILABLE', 'IN_USE', 'MAINTENANCE', 'RETIRED']),
  notes: z.string().max(500).optional(),
});

type ShellFormData = z.infer<typeof shellSchema>;

interface ShellFormProps {
  initialData?: Shell;
  onSubmit: (data: ShellFormData) => void;
  onCancel: () => void;
  isSubmitting: boolean;
}

export function ShellForm({ initialData, onSubmit, onCancel, isSubmitting }: ShellFormProps) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<ShellFormData>({
    resolver: zodResolver(shellSchema),
    defaultValues: initialData ? {
      name: initialData.name,
      boatClass: initialData.boatClass,
      type: initialData.type,
      weightClass: initialData.weightClass,
      rigging: initialData.rigging,
      status: initialData.status,
      notes: initialData.notes || '',
    } : {
      status: 'AVAILABLE',
      type: 'RACING',
      weightClass: 'OPEN',
      rigging: 'STARBOARD',
    },
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      {/* Name field */}
      <div>
        <label className="block text-sm font-medium text-txt-primary mb-1">Name</label>
        <input
          {...register('name')}
          className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary focus:outline-none focus:ring-2 focus:ring-accent-primary"
          disabled={isSubmitting}
        />
        {errors.name && <p className="mt-1 text-sm text-red-500">{errors.name.message}</p>}
      </div>

      {/* Boat class field */}
      <div>
        <label className="block text-sm font-medium text-txt-primary mb-1">Boat Class</label>
        <select
          {...register('boatClass')}
          className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary"
          disabled={isSubmitting}
        >
          <option value="">Select...</option>
          <option value="1x">1x (Single)</option>
          <option value="2-">2- (Pair)</option>
          <option value="2x">2x (Double)</option>
          <option value="4-">4- (Four)</option>
          <option value="4x">4x (Quad)</option>
          <option value="4+">4+ (Coxed Four)</option>
          <option value="8+">8+ (Eight)</option>
        </select>
        {errors.boatClass && <p className="mt-1 text-sm text-red-500">{errors.boatClass.message}</p>}
      </div>

      {/* Type, Weight Class, Rigging selects in grid */}
      <div className="grid grid-cols-3 gap-3">
        <div>
          <label className="block text-sm font-medium text-txt-primary mb-1">Type</label>
          <select {...register('type')} className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary" disabled={isSubmitting}>
            <option value="RACING">Racing</option>
            <option value="TRAINING">Training</option>
            <option value="RECREATIONAL">Recreational</option>
          </select>
        </div>
        <div>
          <label className="block text-sm font-medium text-txt-primary mb-1">Weight</label>
          <select {...register('weightClass')} className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary" disabled={isSubmitting}>
            <option value="OPEN">Open</option>
            <option value="LIGHTWEIGHT">Lightweight</option>
            <option value="HEAVYWEIGHT">Heavyweight</option>
          </select>
        </div>
        <div>
          <label className="block text-sm font-medium text-txt-primary mb-1">Rigging</label>
          <select {...register('rigging')} className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary" disabled={isSubmitting}>
            <option value="STARBOARD">Starboard</option>
            <option value="PORT">Port</option>
            <option value="BUCKET">Bucket</option>
          </select>
        </div>
      </div>

      {/* Status */}
      <div>
        <label className="block text-sm font-medium text-txt-primary mb-1">Status</label>
        <select {...register('status')} className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary" disabled={isSubmitting}>
          <option value="AVAILABLE">Available</option>
          <option value="IN_USE">In Use</option>
          <option value="MAINTENANCE">Maintenance</option>
          <option value="RETIRED">Retired</option>
        </select>
      </div>

      {/* Notes */}
      <div>
        <label className="block text-sm font-medium text-txt-primary mb-1">Notes</label>
        <textarea
          {...register('notes')}
          rows={3}
          className="w-full px-3 py-2 rounded-lg border border-bdr-primary bg-surface text-txt-primary resize-none"
          disabled={isSubmitting}
        />
      </div>

      {/* Actions */}
      <div className="flex justify-end gap-3 pt-4">
        <button
          type="button"
          onClick={onCancel}
          className="px-4 py-2 text-sm border border-bdr-primary rounded-lg hover:bg-surface-hover transition-colors"
          disabled={isSubmitting}
        >
          Cancel
        </button>
        <button
          type="submit"
          className="px-4 py-2 text-sm bg-accent-primary text-white rounded-lg hover:bg-accent-primary/90 transition-colors disabled:opacity-50"
          disabled={isSubmitting}
        >
          {isSubmitting ? 'Saving...' : initialData ? 'Update Shell' : 'Add Shell'}
        </button>
      </div>
    </form>
  );
}
```
  </action>
  <verify>
File exists at src/v2/components/fleet/ShellForm.tsx.
  </verify>
  <done>
ShellForm uses react-hook-form with Zod validation for all shell fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create OarSetForm, ShellsTable, OarsTable</name>
  <files>src/v2/components/fleet/OarSetForm.tsx, src/v2/components/fleet/ShellsTable.tsx, src/v2/components/fleet/OarsTable.tsx, src/v2/components/fleet/index.ts</files>
  <action>
Create src/v2/components/fleet/OarSetForm.tsx with same pattern as ShellForm:
- Zod schema for name, type (SWEEP/SCULL), count (1-100), status, notes
- react-hook-form with zodResolver
- Same styling as ShellForm

Create src/v2/components/fleet/ShellsTable.tsx:
```typescript
import { Shell } from '../../types/coach';
import { Pencil, Trash2 } from 'lucide-react';

interface ShellsTableProps {
  shells: Shell[];
  canEdit: boolean;
  onEdit: (shell: Shell) => void;
  onDelete: (id: string) => void;
}

export function ShellsTable({ shells, canEdit, onEdit, onDelete }: ShellsTableProps) {
  if (shells.length === 0) {
    return (
      <div className="text-center py-12 text-txt-secondary">
        No shells in fleet. {canEdit && 'Add your first shell to get started.'}
      </div>
    );
  }

  return (
    <div className="overflow-x-auto">
      <table className="w-full">
        <thead>
          <tr className="border-b border-bdr-primary">
            <th className="text-left py-3 px-4 text-sm font-medium text-txt-secondary">Name</th>
            <th className="text-left py-3 px-4 text-sm font-medium text-txt-secondary">Class</th>
            <th className="text-left py-3 px-4 text-sm font-medium text-txt-secondary">Type</th>
            <th className="text-left py-3 px-4 text-sm font-medium text-txt-secondary">Status</th>
            {canEdit && <th className="text-right py-3 px-4 text-sm font-medium text-txt-secondary">Actions</th>}
          </tr>
        </thead>
        <tbody>
          {shells.map((shell) => (
            <tr key={shell.id} className="border-b border-bdr-primary/50 hover:bg-surface-hover">
              <td className="py-3 px-4 text-txt-primary font-medium">{shell.name}</td>
              <td className="py-3 px-4 text-txt-secondary">{shell.boatClass}</td>
              <td className="py-3 px-4 text-txt-secondary capitalize">{shell.type.toLowerCase()}</td>
              <td className="py-3 px-4">
                <span className={`inline-flex px-2 py-0.5 text-xs rounded-full ${
                  shell.status === 'AVAILABLE' ? 'bg-green-500/20 text-green-400' :
                  shell.status === 'IN_USE' ? 'bg-blue-500/20 text-blue-400' :
                  shell.status === 'MAINTENANCE' ? 'bg-yellow-500/20 text-yellow-400' :
                  'bg-gray-500/20 text-gray-400'
                }`}>
                  {shell.status.replace('_', ' ')}
                </span>
              </td>
              {canEdit && (
                <td className="py-3 px-4 text-right">
                  <button onClick={() => onEdit(shell)} className="p-1.5 hover:bg-surface-hover rounded-lg mr-1">
                    <Pencil size={16} className="text-txt-secondary" />
                  </button>
                  <button onClick={() => onDelete(shell.id)} className="p-1.5 hover:bg-red-500/20 rounded-lg">
                    <Trash2 size={16} className="text-red-400" />
                  </button>
                </td>
              )}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

Create src/v2/components/fleet/OarsTable.tsx with same pattern for oar sets (columns: Name, Type, Count, Status, Actions).

Create src/v2/components/fleet/index.ts:
```typescript
export { ShellsTable } from './ShellsTable';
export { OarsTable } from './OarsTable';
export { ShellForm } from './ShellForm';
export { OarSetForm } from './OarSetForm';
```
  </action>
  <verify>
1. All files exist in src/v2/components/fleet/
2. Barrel export has all four components
3. Build passes: `npm run build`
  </verify>
  <done>
Fleet components (tables and forms) created with V2 styling and react-hook-form validation.
  </done>
</task>

</tasks>

<verification>
- All components render without errors
- Forms validate using Zod schemas
- Tables display data with proper status badges
- Edit/delete actions only visible when canEdit=true
- V2 design tokens used throughout
- Build passes without TypeScript errors
</verification>

<success_criteria>
- ShellsTable displays shells with status badges
- OarsTable displays oar sets with type and count
- ShellForm validates all required fields
- OarSetForm validates type enum and count range
- CrudModal provides consistent modal experience
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-migration-loop/04-08-SUMMARY.md`
</output>
