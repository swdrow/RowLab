---
phase: 04-migration-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - "react-hook-form is installed and importable"
    - "@hookform/resolvers provides zodResolver function"
    - "@uiw/react-md-editor renders markdown editor"
  artifacts:
    - path: "package.json"
      provides: "New dependencies"
      contains: "react-hook-form"
    - path: "package.json"
      provides: "Zod resolver"
      contains: "@hookform/resolvers"
    - path: "package.json"
      provides: "Markdown editor"
      contains: "@uiw/react-md-editor"
  key_links: []
---

<objective>
Install npm packages required for Phase 4: react-hook-form for forms, @hookform/resolvers for Zod integration, and @uiw/react-md-editor for whiteboard markdown editing.

Purpose: Adds form management and markdown editing capabilities required by coach features.
Output: Updated package.json with new dependencies installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-migration-loop/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 4 npm dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
Run npm install for all three packages in a single command:
```bash
npm install react-hook-form @hookform/resolvers @uiw/react-md-editor
```

Expected versions (from research):
- react-hook-form: ^7.71.1
- @hookform/resolvers: ^3.x
- @uiw/react-md-editor: ^4.x

Do NOT modify any other files. This is purely package installation.
  </action>
  <verify>
1. Run: `npm ls react-hook-form @hookform/resolvers @uiw/react-md-editor`
2. Confirm all three packages are listed with versions
3. Run: `npm run build` to verify no TypeScript/build errors introduced
  </verify>
  <done>
All three packages appear in package.json dependencies and npm ls confirms installation. Build passes without errors.
  </done>
</task>

</tasks>

<verification>
- `npm ls react-hook-form` shows installed version
- `npm ls @hookform/resolvers` shows installed version
- `npm ls @uiw/react-md-editor` shows installed version
- `npm run build` completes without errors
</verification>

<success_criteria>
- react-hook-form ^7.x installed
- @hookform/resolvers ^3.x installed
- @uiw/react-md-editor ^4.x installed
- Build still passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-migration-loop/04-01-SUMMARY.md`
</output>
