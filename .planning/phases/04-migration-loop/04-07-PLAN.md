---
phase: 04-migration-loop
plan: 07
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - src/v2/components/whiteboard/WhiteboardView.tsx
  - src/v2/components/whiteboard/WhiteboardEditor.tsx
  - src/v2/components/whiteboard/index.ts
autonomous: true

must_haves:
  truths:
    - "WhiteboardView displays markdown content with author and date"
    - "WhiteboardEditor uses @uiw/react-md-editor for markdown editing"
    - "Coaches see edit button, athletes only see view"
    - "Empty state shown when no whiteboard exists"
  artifacts:
    - path: "src/v2/components/whiteboard/WhiteboardView.tsx"
      provides: "Whiteboard display component"
      exports: ["WhiteboardView"]
    - path: "src/v2/components/whiteboard/WhiteboardEditor.tsx"
      provides: "Markdown editor for whiteboard"
      exports: ["WhiteboardEditor"]
    - path: "src/v2/components/whiteboard/index.ts"
      provides: "Barrel exports"
      exports: ["WhiteboardView", "WhiteboardEditor"]
  key_links:
    - from: "src/v2/components/whiteboard/WhiteboardEditor.tsx"
      to: "@uiw/react-md-editor"
      via: "MDEditor import"
      pattern: "import MDEditor from '@uiw/react-md-editor'"
    - from: "src/v2/components/whiteboard/WhiteboardView.tsx"
      to: "src/v2/hooks/useWhiteboard.ts"
      via: "hook import"
      pattern: "import.*useWhiteboard"
---

<objective>
Create whiteboard UI components for daily team communications. View displays rendered markdown, Editor provides rich markdown editing with live preview.

Purpose: Coach-to-athlete communication via daily whiteboard posts (COACH-01, COACH-02).
Output: WhiteboardView and WhiteboardEditor components with V2 design tokens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-migration-loop/04-RESEARCH.md
@src/v2/components/dashboard/ActivityCard.tsx (V2 styling reference)
@src/v2/hooks/useWhiteboard.ts (from 04-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhiteboardView component</name>
  <files>src/v2/components/whiteboard/WhiteboardView.tsx</files>
  <action>
Create src/v2/components/whiteboard/WhiteboardView.tsx:

```typescript
import { Whiteboard } from '../../types/coach';

interface WhiteboardViewProps {
  whiteboard: Whiteboard | null;
  canEdit: boolean;
  onEdit?: () => void;
}

export function WhiteboardView({ whiteboard, canEdit, onEdit }: WhiteboardViewProps) {
  // Empty state when no whiteboard
  if (!whiteboard) {
    return (
      <div className="rounded-xl border border-bdr-primary bg-card-bg p-8 text-center">
        <div className="text-txt-secondary mb-4">
          <svg className="w-12 h-12 mx-auto mb-2 opacity-50" /* clipboard icon */ />
          No whiteboard posted yet
        </div>
        {canEdit && onEdit && (
          <button
            onClick={onEdit}
            className="px-4 py-2 bg-accent-primary text-white rounded-lg hover:bg-accent-primary/90 transition-colors"
          >
            Create Today's Whiteboard
          </button>
        )}
      </div>
    );
  }

  // Format date for display
  const displayDate = new Date(whiteboard.date).toLocaleDateString('en-US', {
    weekday: 'long',
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });

  return (
    <div className="rounded-xl border border-bdr-primary bg-card-bg overflow-hidden">
      {/* Header with date and author */}
      <div className="flex items-center justify-between px-6 py-4 border-b border-bdr-primary bg-surface">
        <div>
          <h2 className="text-lg font-semibold text-txt-primary">{displayDate}</h2>
          <p className="text-sm text-txt-secondary">Posted by {whiteboard.author.name}</p>
        </div>
        {canEdit && onEdit && (
          <button
            onClick={onEdit}
            className="px-3 py-1.5 text-sm border border-bdr-primary rounded-lg hover:bg-surface-hover transition-colors"
          >
            Edit
          </button>
        )}
      </div>

      {/* Markdown content - use MDEditor.Markdown for rendering */}
      <div className="p-6 prose prose-invert max-w-none">
        {/* Import and use MDEditor.Markdown for consistent rendering */}
        <div
          className="whiteboard-content"
          dangerouslySetInnerHTML={{ __html: '' }} /* placeholder - use MDEditor.Markdown */
        />
      </div>
    </div>
  );
}
```

Use @uiw/react-md-editor's Markdown component for rendering:
```typescript
import MDEditor from '@uiw/react-md-editor';

// In render:
<MDEditor.Markdown source={whiteboard.content} />
```

Style the markdown content with V2 tokens. Add a Lucide icon (ClipboardList or similar) for the empty state.
  </action>
  <verify>
File exists at src/v2/components/whiteboard/WhiteboardView.tsx.
  </verify>
  <done>
WhiteboardView displays whiteboard content with header and edit button for coaches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create WhiteboardEditor component</name>
  <files>src/v2/components/whiteboard/WhiteboardEditor.tsx</files>
  <action>
Create src/v2/components/whiteboard/WhiteboardEditor.tsx using @uiw/react-md-editor:

```typescript
import { useState } from 'react';
import MDEditor from '@uiw/react-md-editor';

interface WhiteboardEditorProps {
  initialContent?: string;
  onSave: (content: string) => void;
  onCancel: () => void;
  isSaving: boolean;
}

export function WhiteboardEditor({ initialContent = '', onSave, onCancel, isSaving }: WhiteboardEditorProps) {
  const [content, setContent] = useState(initialContent);

  const handleSave = () => {
    if (content.trim()) {
      onSave(content);
    }
  };

  return (
    <div className="rounded-xl border border-bdr-primary bg-card-bg overflow-hidden">
      {/* Header */}
      <div className="px-6 py-4 border-b border-bdr-primary bg-surface">
        <h2 className="text-lg font-semibold text-txt-primary">Edit Whiteboard</h2>
        <p className="text-sm text-txt-secondary">
          {new Date().toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' })}
        </p>
      </div>

      {/* Editor */}
      <div className="p-6" data-color-mode="dark">
        <MDEditor
          value={content}
          onChange={(val) => setContent(val || '')}
          height={400}
          preview="live"
          textareaProps={{
            placeholder: 'Enter today\'s whiteboard content (markdown supported)...',
          }}
        />
      </div>

      {/* Actions */}
      <div className="flex justify-end gap-3 px-6 py-4 border-t border-bdr-primary bg-surface">
        <button
          type="button"
          onClick={onCancel}
          className="px-4 py-2 text-sm border border-bdr-primary rounded-lg hover:bg-surface-hover transition-colors"
          disabled={isSaving}
        >
          Cancel
        </button>
        <button
          type="button"
          onClick={handleSave}
          className="px-4 py-2 text-sm bg-accent-primary text-white rounded-lg hover:bg-accent-primary/90 transition-colors disabled:opacity-50"
          disabled={isSaving || !content.trim()}
        >
          {isSaving ? 'Saving...' : 'Post Whiteboard'}
        </button>
      </div>
    </div>
  );
}
```

Key details:
- Use data-color-mode="dark" on the editor wrapper for proper theming
- MDEditor.preview="live" shows side-by-side editing
- Disable save button when content empty or saving in progress
- V2 design tokens for all styling (bg-card-bg, text-txt-primary, etc.)
  </action>
  <verify>
File exists at src/v2/components/whiteboard/WhiteboardEditor.tsx.
  </verify>
  <done>
WhiteboardEditor provides markdown editing with live preview, save/cancel actions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export</name>
  <files>src/v2/components/whiteboard/index.ts</files>
  <action>
Create src/v2/components/whiteboard/index.ts:

```typescript
export { WhiteboardView } from './WhiteboardView';
export { WhiteboardEditor } from './WhiteboardEditor';
```

Verify directory structure:
```
src/v2/components/whiteboard/
  ├── WhiteboardView.tsx
  ├── WhiteboardEditor.tsx
  └── index.ts
```
  </action>
  <verify>
1. Directory structure correct
2. Build passes: `npm run build`
  </verify>
  <done>
Whiteboard components exported via barrel file.
  </done>
</task>

</tasks>

<verification>
- Both components render without errors
- MDEditor integrates correctly with dark theme
- V2 design tokens used throughout
- Build passes without TypeScript errors
</verification>

<success_criteria>
- WhiteboardView displays markdown content with date and author
- WhiteboardEditor provides live markdown editing
- Empty state handles no whiteboard case
- Edit button only visible to coaches (canEdit prop)
- V2 styling consistent with existing components
</success_criteria>

<output>
After completion, create `.planning/phases/04-migration-loop/04-07-SUMMARY.md`
</output>
