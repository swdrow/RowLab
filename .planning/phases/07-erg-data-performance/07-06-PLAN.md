---
phase: 07-erg-data-performance
plan: 06
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All erg test features verified working in browser"
    - "CRUD operations confirmed functional"
    - "Charts render correctly with real data"
    - "CSV import tested with sample file"
---

<objective>
Human verification checkpoint for all Phase 7 erg data features.

Purpose: Ensure all components work correctly together before marking the phase complete.

Output: Verified phase completion or identified issues for fixing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete erg data and performance tracking system:
- Team erg tests table with sorting and filtering
- Add/edit/delete erg test forms
- Athlete erg history with trend charts
- Personal bests display
- CSV bulk import wizard
- Concept2 connection status display
- Manual C2 sync trigger
  </what-built>
  <how-to-verify>
**1. Erg Tests Page (ERG-01, ERG-02, ERG-03, ERG-04):**
   - Navigate to /app/erg-tests (or equivalent route)
   - Verify table loads with existing tests (or empty state if none)
   - Try filtering by test type (2k, 6k)
   - Try filtering by date range
   - Click "Add Test" and create a new test with all fields
   - Verify new test appears in table
   - Click edit on a test, modify time, save
   - Verify changes reflected
   - Click delete on a test, confirm deletion
   - Verify test removed from table

**2. Athlete Erg History (ERG-05, ERG-06):**
   - Navigate to an athlete's profile or erg history view
   - Verify chart displays with test data points
   - Toggle between test types (2k, 6k, etc.)
   - Verify personal bests card shows correct times
   - Check tooltip on chart shows test details

**3. CSV Import (ERG-07):**
   - Click "Import CSV" button
   - Upload test file with columns: Athlete Name, Date, Test Type, Time
   - Verify column auto-mapping suggests correct fields
   - Adjust mapping if needed
   - Check preview shows valid/invalid row counts
   - Click import
   - Verify tests appear in table after import

**4. Concept2 Status (ERG-08, ERG-09):**
   - View C2 status section (on erg tests page or settings)
   - Check connected athletes show green badge
   - Check not connected athletes show gray badge
   - Verify last sync time shows relative time
   - Click sync button for a connected athlete
   - Verify loading state during sync
   - Verify success message after sync
   - (If available) Try sync all button

**5. General UX:**
   - Verify loading states show spinners
   - Verify error states display helpful messages
   - Check responsive layout on narrow viewport
   - Verify optimistic updates (no page refresh needed)

URL: http://100.86.4.57:3001/app/erg-tests (or check router for actual route)
  </how-to-verify>
  <resume-signal>
Type "approved" if all features work correctly.
Or describe specific issues found for fixing.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification checkpoint - no automated verification.
</verification>

<success_criteria>
- [ ] All ERG requirements (ERG-01 through ERG-09) verified working
- [ ] No critical bugs in CRUD operations
- [ ] Charts render correctly
- [ ] CSV import processes test files correctly
- [ ] C2 status and sync features functional
</success_criteria>

<output>
After completion, create `.planning/phases/07-erg-data-performance/07-06-SUMMARY.md`

Document any issues found and fixes applied during verification.
</output>
