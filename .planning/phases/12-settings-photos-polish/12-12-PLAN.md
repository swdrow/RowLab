---
phase: 12-settings-photos-polish
plan: 12
type: execute
wave: 4
depends_on: ["12-08"]
files_modified:
  - src/v2/styles/themes/dark.css
  - src/v2/styles/themes/light.css
  - src/v2/styles/themes/field.css
  - src/v2/styles/focus-rings.css
  - src/v2/styles/reduced-motion.css
autonomous: true

must_haves:
  truths:
    - "Dark theme renders all components correctly"
    - "Light theme has no CSS cascade issues"
    - "Field theme is high-contrast and outdoor readable"
    - "Focus rings visible on all interactive elements"
    - "prefers-reduced-motion disables animations"
  artifacts:
    - path: "src/v2/styles/focus-rings.css"
      provides: "Focus ring styles for accessibility"
      exports: []
    - path: "src/v2/styles/reduced-motion.css"
      provides: "Reduced motion media query styles"
      exports: []
  key_links:
    - from: "focus-rings.css"
      to: "all interactive elements"
      via: ":focus-visible pseudo-class"
      pattern: ":focus-visible"
---

<objective>
Complete theme polish and accessibility improvements for dark, light, and field themes.

Purpose: Deliver POLISH-09 (theme consistency), POLISH-11 (accessibility - focus rings, reduced motion).
Output: All themes render correctly with full accessibility support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-settings-photos-polish/12-08-SUMMARY.md
@src/v2/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix theme CSS</name>
  <files>
    src/v2/styles/themes/dark.css
    src/v2/styles/themes/light.css
    src/v2/styles/themes/field.css
  </files>
  <action>
Review and fix theme-specific CSS files to ensure complete token coverage.

**For each theme file, verify:**

1. All CSS custom properties from tokens.css are defined
2. Colors have appropriate values for the theme
3. No missing variables that would fall back to wrong values

**dark.css checklist:**
```css
[data-theme="dark"] {
  /* Backgrounds */
  --color-bg-base: #0A0A0A;
  --color-bg-surface: #171717;
  --color-bg-elevated: #262626;
  --color-bg-hover: #333333;

  /* Text */
  --color-text-primary: #FAFAFA;
  --color-text-secondary: #A3A3A3;
  --color-text-tertiary: #737373;

  /* Borders */
  --color-border-subtle: rgba(255, 255, 255, 0.06);
  --color-border-default: rgba(255, 255, 255, 0.10);
  --color-border-brand: var(--color-interactive-primary);

  /* Interactive */
  --color-interactive-primary: #0070F3;
  --color-interactive-hover: #2584FF;
  --color-interactive-disabled: #525252;

  /* Shadows should use rgba with appropriate alpha */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.4);
  /* ... etc */
}
```

**light.css checklist:**
```css
[data-theme="light"] {
  /* Backgrounds - invert darkness */
  --color-bg-base: #FFFFFF;
  --color-bg-surface: #F5F5F5;
  --color-bg-elevated: #FFFFFF;
  --color-bg-hover: #F0F0F0;

  /* Text - dark on light */
  --color-text-primary: #171717;
  --color-text-secondary: #525252;
  --color-text-tertiary: #737373;

  /* Borders - solid grays instead of rgba */
  --color-border-subtle: #E5E5E5;
  --color-border-default: #D4D4D4;

  /* Shadows should be lighter */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}
```

**field.css checklist (high contrast outdoor):**
```css
[data-theme="field"] {
  /* Amber/yellow backgrounds for outdoor visibility */
  --color-bg-base: #fef3c7; /* amber-100 */
  --color-bg-surface: #fef08a; /* amber-200 */
  --color-bg-elevated: #fde047; /* amber-300 */
  --color-bg-hover: #fcd34d; /* amber-400 */

  /* Very dark text for contrast on amber */
  --color-text-primary: #1c1917; /* stone-900 */
  --color-text-secondary: #44403c; /* stone-700 */
  --color-text-tertiary: #78716c; /* stone-500 */

  /* Darker borders for visibility */
  --color-border-subtle: #d6d3d1; /* stone-300 */
  --color-border-default: #a8a29e; /* stone-400 */

  /* Amber interactive colors */
  --color-interactive-primary: #b45309; /* amber-700 */
  --color-interactive-hover: #92400e; /* amber-800 */

  /* High contrast status colors */
  --color-status-error: #dc2626; /* red-600 */
  --color-status-success: #15803d; /* green-700 */
}
```

Fix any missing tokens found during audit.
  </action>
  <verify>Switch between themes in browser, verify no visual bugs or missing colors</verify>
  <done>All three themes have complete token coverage and render correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create focus ring styles</name>
  <files>src/v2/styles/focus-rings.css</files>
  <action>
Create comprehensive focus ring styles for accessibility (WCAG 2.1 AA).

**focus-rings.css:**
```css
/**
 * Focus ring styles for keyboard navigation accessibility
 * WCAG 2.1 AA requires visible focus indicators on interactive elements
 *
 * Uses :focus-visible to show focus only for keyboard navigation,
 * not mouse clicks (unlike :focus)
 */

/* Remove default browser focus (will be replaced with :focus-visible) */
.v2 *:focus {
  outline: none;
}

/* Default focus ring for all interactive elements */
.v2 *:focus-visible {
  outline: 2px solid var(--color-interactive-primary);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}

/* Button-specific focus ring with glow */
.v2 button:focus-visible,
.v2 [role="button"]:focus-visible {
  outline-color: var(--color-interactive-primary);
  box-shadow: 0 0 0 4px rgba(0, 112, 243, 0.15);
}

/* Input focus - inset ring */
.v2 input:focus-visible,
.v2 textarea:focus-visible,
.v2 select:focus-visible {
  outline: none;
  border-color: var(--color-interactive-primary);
  box-shadow: 0 0 0 3px rgba(0, 112, 243, 0.1);
}

/* Card/panel focus - thicker outline */
.v2 [role="listitem"]:focus-visible,
.v2 [role="option"]:focus-visible,
.v2 [role="row"]:focus-visible {
  outline-width: 2px;
  outline-offset: -2px; /* Inside the element */
}

/* Link focus */
.v2 a:focus-visible {
  outline-offset: 4px;
  border-radius: var(--radius-sm);
}

/* Tab focus */
.v2 [role="tab"]:focus-visible {
  outline-offset: -4px;
  z-index: 1;
}

/* Toggle/switch focus */
.v2 [role="switch"]:focus-visible {
  outline-offset: 2px;
}

/* Dark theme adjustments */
.v2[data-theme="dark"] *:focus-visible {
  outline-color: var(--color-interactive-primary);
}

.v2[data-theme="dark"] button:focus-visible {
  box-shadow: 0 0 0 4px rgba(0, 112, 243, 0.25);
}

/* Light theme adjustments */
.v2[data-theme="light"] *:focus-visible {
  outline-color: var(--color-interactive-primary);
}

/* Field theme - thicker rings for outdoor visibility */
.v2[data-theme="field"] *:focus-visible {
  outline-width: 3px;
  outline-color: var(--color-interactive-primary);
}

.v2[data-theme="field"] button:focus-visible {
  box-shadow: 0 0 0 5px rgba(180, 83, 9, 0.3);
}

/* Skip link for screen readers */
.v2 .skip-link {
  position: absolute;
  top: -100px;
  left: 50%;
  transform: translateX(-50%);
  padding: 8px 16px;
  background: var(--color-bg-surface);
  border: 2px solid var(--color-border-default);
  border-radius: var(--radius-md);
  z-index: 100;
  transition: top 0.2s;
}

.v2 .skip-link:focus {
  top: 8px;
}
```

Import this file in the main V2 styles entry point.
  </action>
  <verify>Tab through pages, verify focus rings visible on all interactive elements</verify>
  <done>Focus ring styles created for all interactive elements and themes</done>
</task>

<task type="auto">
  <name>Task 3: Create reduced motion styles</name>
  <files>src/v2/styles/reduced-motion.css</files>
  <action>
Create reduced motion support for users who prefer no animations.

**reduced-motion.css:**
```css
/**
 * Reduced motion support for accessibility
 * WCAG 2.1 Success Criterion 2.3.3 (AAA) and 2.2.2 (A)
 *
 * @media (prefers-reduced-motion: reduce) disables or minimizes
 * motion for users who may experience vestibular disorders or
 * motion sensitivity.
 */

@media (prefers-reduced-motion: reduce) {
  /* Global: disable all animations and transitions */
  .v2 *,
  .v2 *::before,
  .v2 *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }

  /* Keep minimal feedback animations (essential for UX) */
  .v2 button,
  .v2 [role="button"] {
    /* Allow instant color changes, just no motion */
    transition: background-color 0.1ms !important;
  }

  /* Disable skeleton shimmer animation */
  .v2 .react-loading-skeleton {
    animation: none !important;
  }

  /* Disable Framer Motion animations via CSS */
  .v2 [data-framer-animation] {
    transform: none !important;
    opacity: 1 !important;
  }

  /* Disable page transitions */
  .v2 [data-page-transition] {
    transform: none !important;
    opacity: 1 !important;
  }

  /* Disable drag animations */
  .v2 [data-dragging="true"] {
    transform: none !important;
    box-shadow: none !important;
  }

  /* Disable modal backdrop animation */
  .v2 [data-modal-backdrop] {
    backdrop-filter: none !important;
    opacity: 1 !important;
  }

  /* Disable toast entrance/exit */
  .v2 [data-toast] {
    transform: none !important;
    opacity: 1 !important;
  }

  /* Keep loading spinners but make them static indicator */
  .v2 .animate-spin {
    animation: none !important;
    /* Replace with static loading indicator */
    opacity: 0.5;
  }

  /* Disable hover scale effects */
  .v2 button:hover,
  .v2 [role="button"]:hover {
    transform: none !important;
  }

  /* Disable carousel/slider motion */
  .v2 [data-carousel] {
    scroll-snap-type: none !important;
    overflow: auto !important;
  }
}

/* Provide alternative loading indication when animations disabled */
@media (prefers-reduced-motion: reduce) {
  .v2 [aria-busy="true"]::after {
    content: "Loading...";
    display: inline-block;
    margin-left: 8px;
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }
}
```

Import this file after animations.css in the V2 styles entry point.

Also ensure usePrefersReducedMotion hook from animations.ts is used in all
Framer Motion components to disable animations at the JavaScript level.
  </action>
  <verify>
Enable reduced motion in OS settings, verify:
1. No animations play
2. Page transitions are instant
3. Modals appear/disappear instantly
4. Drag and drop still works (just no motion)
  </verify>
  <done>Reduced motion styles and support complete</done>
</task>

</tasks>

<verification>
1. Dark theme: All components visible, no white-on-white or black-on-black text
2. Light theme: No CSS cascade issues, text readable
3. Field theme: High contrast, readable in bright light
4. Focus rings: Tab through entire app, focus visible on every interactive element
5. Reduced motion: Enable in OS, verify no animations play
6. Test in Chrome DevTools with forced prefers-reduced-motion
</verification>

<success_criteria>
- POLISH-09: All three themes render correctly without CSS issues
- POLISH-11 (partial): Focus rings visible on all interactive elements
- POLISH-11 (partial): prefers-reduced-motion support implemented
- Field theme meets outdoor readability requirements
- Skip link available for screen reader users
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings-photos-polish/12-12-SUMMARY.md`
</output>
