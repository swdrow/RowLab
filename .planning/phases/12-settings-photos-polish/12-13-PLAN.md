---
phase: 12-settings-photos-polish
plan: 13
type: execute
wave: 4
depends_on: ["12-08"]
files_modified:
  - src/v2/styles/responsive.css
  - src/v2/components/shell/MobileNav.tsx
  - src/v2/hooks/useBreakpoint.ts
autonomous: true

must_haves:
  truths:
    - "All pages tested at 375px, 768px, 1024px breakpoints"
    - "Mobile navigation is touch-friendly with 44px minimum tap targets"
    - "Tables convert to cards or scroll horizontally on mobile"
    - "Forms remain usable on small screens"
    - "No horizontal overflow at any breakpoint"
  artifacts:
    - path: "src/v2/hooks/useBreakpoint.ts"
      provides: "Responsive breakpoint detection hook"
      exports: ["useBreakpoint", "Breakpoint"]
    - path: "src/v2/components/shell/MobileNav.tsx"
      provides: "Mobile-optimized navigation"
      exports: ["MobileNav"]
  key_links:
    - from: "useBreakpoint"
      to: "all responsive components"
      via: "conditional rendering"
      pattern: "useBreakpoint|breakpoint.*===.*mobile"
---

<objective>
Audit and fix responsive behavior at 375px, 768px, and 1024px breakpoints.

Purpose: Deliver POLISH-10 (responsive audit at all breakpoints).
Output: All pages work correctly on mobile, tablet, and desktop.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-settings-photos-polish/12-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useBreakpoint hook and responsive utilities</name>
  <files>
    src/v2/hooks/useBreakpoint.ts
    src/v2/styles/responsive.css
  </files>
  <action>
**useBreakpoint.ts:**
```typescript
import { useState, useEffect } from 'react';

export type Breakpoint = 'mobile' | 'tablet' | 'desktop';

/**
 * Standard breakpoints matching design system
 * Mobile: 0 - 767px
 * Tablet: 768px - 1023px
 * Desktop: 1024px+
 */
export const BREAKPOINTS = {
  mobile: 0,
  tablet: 768,
  desktop: 1024,
} as const;

/**
 * Hook to detect current breakpoint
 * Returns 'mobile' | 'tablet' | 'desktop'
 */
export const useBreakpoint = (): Breakpoint => {
  const [breakpoint, setBreakpoint] = useState<Breakpoint>(() => {
    if (typeof window === 'undefined') return 'desktop';
    return getBreakpoint(window.innerWidth);
  });

  useEffect(() => {
    const handleResize = () => {
      setBreakpoint(getBreakpoint(window.innerWidth));
    };

    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return breakpoint;
};

function getBreakpoint(width: number): Breakpoint {
  if (width < BREAKPOINTS.tablet) return 'mobile';
  if (width < BREAKPOINTS.desktop) return 'tablet';
  return 'desktop';
}

/**
 * Hook to check if current viewport is mobile
 */
export const useIsMobile = (): boolean => {
  const breakpoint = useBreakpoint();
  return breakpoint === 'mobile';
};

/**
 * Hook to check if current viewport is tablet or smaller
 */
export const useIsTabletOrSmaller = (): boolean => {
  const breakpoint = useBreakpoint();
  return breakpoint === 'mobile' || breakpoint === 'tablet';
};

/**
 * Hook to get if we should show mobile layout
 * (mobile navigation, simplified layouts)
 */
export const useShowMobileLayout = (): boolean => {
  return useIsMobile();
};
```

**responsive.css:**
```css
/**
 * Responsive utility styles
 * Breakpoints: 375px (mobile), 768px (tablet), 1024px (desktop)
 */

/* Hide on specific breakpoints */
@media (max-width: 767px) {
  .v2 .hide-mobile {
    display: none !important;
  }
}

@media (min-width: 768px) and (max-width: 1023px) {
  .v2 .hide-tablet {
    display: none !important;
  }
}

@media (min-width: 1024px) {
  .v2 .hide-desktop {
    display: none !important;
  }
}

/* Show only on specific breakpoints */
@media (min-width: 768px) {
  .v2 .show-mobile-only {
    display: none !important;
  }
}

@media (max-width: 767px) {
  .v2 .show-tablet-up {
    display: none !important;
  }
}

/* Touch-friendly tap targets (44px minimum per WCAG) */
.v2 .tap-target {
  min-width: 44px;
  min-height: 44px;
}

/* Mobile-first grid utilities */
.v2 .mobile-stack {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-4);
}

@media (min-width: 768px) {
  .v2 .mobile-stack.tablet-row {
    flex-direction: row;
  }
}

/* Responsive table - horizontal scroll on mobile */
.v2 .responsive-table-container {
  width: 100%;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

@media (max-width: 767px) {
  .v2 .responsive-table-container {
    margin: 0 calc(var(--spacing-4) * -1);
    padding: 0 var(--spacing-4);
    width: calc(100% + var(--spacing-4) * 2);
  }
}

/* Card view for tables on mobile */
@media (max-width: 767px) {
  .v2 .table-to-cards thead {
    display: none;
  }

  .v2 .table-to-cards tbody tr {
    display: block;
    padding: var(--spacing-4);
    margin-bottom: var(--spacing-3);
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-lg);
    background: var(--color-bg-surface);
  }

  .v2 .table-to-cards tbody td {
    display: flex;
    justify-content: space-between;
    padding: var(--spacing-2) 0;
    border: none;
  }

  .v2 .table-to-cards tbody td::before {
    content: attr(data-label);
    font-weight: 500;
    color: var(--color-text-secondary);
  }
}

/* Mobile-friendly form layouts */
@media (max-width: 767px) {
  .v2 .form-row {
    flex-direction: column;
  }

  .v2 .form-row > * {
    width: 100%;
  }

  .v2 .button-group {
    flex-direction: column;
  }

  .v2 .button-group > button {
    width: 100%;
  }
}

/* Fixed bottom action bar for mobile */
.v2 .mobile-action-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: var(--spacing-4);
  background: var(--color-bg-surface);
  border-top: 1px solid var(--color-border-subtle);
  z-index: 40;
}

@media (min-width: 768px) {
  .v2 .mobile-action-bar {
    position: static;
    padding: 0;
    background: transparent;
    border: none;
  }
}

/* Safe area insets for notched phones */
@supports (padding-bottom: env(safe-area-inset-bottom)) {
  .v2 .mobile-action-bar {
    padding-bottom: calc(var(--spacing-4) + env(safe-area-inset-bottom));
  }
}
```
  </action>
  <verify>useBreakpoint returns correct value when resizing browser</verify>
  <done>Breakpoint detection and responsive utilities created</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile navigation component</name>
  <files>src/v2/components/shell/MobileNav.tsx</files>
  <action>
**MobileNav.tsx:**
```typescript
import { useState } from 'react';
import { Link, useLocation } from 'react-router-dom';
import { motion, AnimatePresence } from 'framer-motion';
import {
  Menu,
  X,
  Home,
  Users,
  Activity,
  Ship,
  Timer,
  Calendar,
  Trophy,
  Settings,
} from 'lucide-react';
import { SPRING_CONFIG, usePrefersReducedMotion } from '@v2/utils/animations';
import { cn } from '@v2/utils/cn';
import { useShowMobileLayout } from '@v2/hooks/useBreakpoint';

interface NavItem {
  label: string;
  path: string;
  icon: React.ComponentType<{ className?: string }>;
}

const navItems: NavItem[] = [
  { label: 'Dashboard', path: '/app', icon: Home },
  { label: 'Athletes', path: '/app/athletes', icon: Users },
  { label: 'Erg Data', path: '/app/erg', icon: Activity },
  { label: 'Lineups', path: '/app/lineups', icon: Ship },
  { label: 'Seat Racing', path: '/app/seat-racing', icon: Timer },
  { label: 'Training', path: '/app/training', icon: Calendar },
  { label: 'Regattas', path: '/app/regattas', icon: Trophy },
  { label: 'Settings', path: '/app/settings', icon: Settings },
];

export const MobileNav: React.FC = () => {
  const [isOpen, setIsOpen] = useState(false);
  const location = useLocation();
  const showMobile = useShowMobileLayout();
  const prefersReducedMotion = usePrefersReducedMotion();

  if (!showMobile) return null;

  return (
    <>
      {/* Mobile header with hamburger */}
      <header className="fixed top-0 left-0 right-0 h-14 bg-surface border-b border-bdr-subtle z-50 flex items-center justify-between px-4">
        <Link to="/app" className="text-lg font-bold text-txt-primary">
          RowLab
        </Link>
        <button
          onClick={() => setIsOpen(!isOpen)}
          className="tap-target flex items-center justify-center"
          aria-label={isOpen ? 'Close menu' : 'Open menu'}
          aria-expanded={isOpen}
        >
          {isOpen ? (
            <X className="w-6 h-6 text-txt-primary" />
          ) : (
            <Menu className="w-6 h-6 text-txt-primary" />
          )}
        </button>
      </header>

      {/* Mobile menu overlay */}
      <AnimatePresence>
        {isOpen && (
          <>
            {/* Backdrop */}
            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              exit={{ opacity: 0 }}
              transition={{ duration: prefersReducedMotion ? 0 : 0.2 }}
              className="fixed inset-0 bg-black/50 z-40"
              onClick={() => setIsOpen(false)}
            />

            {/* Slide-in menu */}
            <motion.nav
              initial={{ x: '-100%' }}
              animate={{ x: 0 }}
              exit={{ x: '-100%' }}
              transition={prefersReducedMotion ? { duration: 0 } : SPRING_CONFIG}
              className="fixed top-14 left-0 bottom-0 w-64 bg-surface border-r border-bdr-subtle z-50 overflow-y-auto"
            >
              <ul className="py-4">
                {navItems.map((item) => {
                  const isActive = location.pathname === item.path;
                  const Icon = item.icon;

                  return (
                    <li key={item.path}>
                      <Link
                        to={item.path}
                        onClick={() => setIsOpen(false)}
                        className={cn(
                          'flex items-center gap-3 px-4 py-3 tap-target',
                          'transition-colors',
                          isActive
                            ? 'bg-interactive-primary/10 text-interactive-primary border-l-4 border-interactive-primary'
                            : 'text-txt-secondary hover:bg-hover hover:text-txt-primary'
                        )}
                      >
                        <Icon className="w-5 h-5" />
                        <span className="font-medium">{item.label}</span>
                      </Link>
                    </li>
                  );
                })}
              </ul>
            </motion.nav>
          </>
        )}
      </AnimatePresence>

      {/* Bottom tab bar for quick access */}
      <nav className="fixed bottom-0 left-0 right-0 h-16 bg-surface border-t border-bdr-subtle z-50 safe-area-inset-bottom">
        <ul className="flex items-center justify-around h-full">
          {navItems.slice(0, 5).map((item) => {
            const isActive = location.pathname === item.path;
            const Icon = item.icon;

            return (
              <li key={item.path}>
                <Link
                  to={item.path}
                  className={cn(
                    'flex flex-col items-center justify-center gap-1 tap-target px-3',
                    isActive ? 'text-interactive-primary' : 'text-txt-tertiary'
                  )}
                >
                  <Icon className="w-5 h-5" />
                  <span className="text-xs">{item.label.split(' ')[0]}</span>
                </Link>
              </li>
            );
          })}
        </ul>
      </nav>
    </>
  );
};

// Spacer to prevent content from being hidden behind fixed nav
export const MobileNavSpacer: React.FC = () => {
  const showMobile = useShowMobileLayout();
  if (!showMobile) return null;
  return <div className="h-14" />; // Top spacer
};

export const MobileNavBottomSpacer: React.FC = () => {
  const showMobile = useShowMobileLayout();
  if (!showMobile) return null;
  return <div className="h-16" />; // Bottom spacer
};
```

Features:
- Hamburger menu with slide-in drawer
- Bottom tab bar for quick access (5 main items)
- 44px minimum tap targets
- Safe area insets for notched phones
- Active state highlighting
- Backdrop closes menu
- Keyboard accessible
  </action>
  <verify>Mobile nav renders at 375px width, tap targets are 44px minimum</verify>
  <done>Mobile navigation component created</done>
</task>

<task type="auto">
  <name>Task 3: Document responsive audit checklist</name>
  <files>src/v2/styles/responsive-audit.md</files>
  <action>
Create a responsive audit checklist and document findings.

**responsive-audit.md:**
```markdown
# Responsive Audit Checklist

**Date:** 2026-01-25
**Phase:** 12-settings-photos-polish

## Breakpoints Tested

- [ ] 375px (iPhone SE/small phones)
- [ ] 414px (iPhone 14/Plus)
- [ ] 768px (iPad mini/tablets)
- [ ] 1024px (iPad Pro/small laptops)
- [ ] 1280px (laptops)
- [ ] 1440px (desktops)

## Page-by-Page Audit

### Dashboard (/app)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | |
| 768px | | |
| 1024px | | |

### Athletes (/app/athletes)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | Table converts to cards |
| 768px | | Grid 2 columns |
| 1024px | | Full table, sidebar |

### Erg Data (/app/erg)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | Horizontal scroll table |
| 768px | | |
| 1024px | | |

### Lineups (/app/lineups)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | Single column |
| 768px | | |
| 1024px | | Side-by-side bank + boat |

### Seat Racing (/app/seat-racing)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | |
| 768px | | |
| 1024px | | |

### Training (/app/training)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | Week view vertical |
| 768px | | Week view horizontal |
| 1024px | | Month view |

### Regattas (/app/regattas)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | |
| 768px | | |
| 1024px | | |

### Settings (/app/settings)
| Breakpoint | Status | Notes |
|------------|--------|-------|
| 375px | | Tabs stack vertically |
| 768px | | |
| 1024px | | |

## Common Issues to Check

- [ ] No horizontal overflow (scroll left/right)
- [ ] Text doesn't wrap awkwardly
- [ ] Buttons are at least 44px tap targets
- [ ] Forms have adequate spacing
- [ ] Modals fit within viewport
- [ ] Fixed elements don't overlap content
- [ ] Images scale appropriately
- [ ] Tables don't break layout

## Fixes Applied

| Page | Issue | Fix |
|------|-------|-----|
| | | |

## Deferred Issues

| Page | Issue | Reason |
|------|-------|--------|
| | | |
```

This document will be filled in during manual testing.
The executor should use Chrome DevTools device emulation to test each page.
  </action>
  <verify>Audit document exists and has checklist structure</verify>
  <done>Responsive audit checklist documented</done>
</task>

</tasks>

<verification>
1. useBreakpoint hook works correctly at 375px, 768px, 1024px
2. Mobile nav appears at <768px with hamburger and bottom tabs
3. Tap targets are 44px minimum (verify with DevTools)
4. No horizontal scroll on any page at 375px
5. Tables convert to cards or scroll horizontally on mobile
</verification>

<success_criteria>
- POLISH-10: All pages tested at 375px, 768px, 1024px
- Mobile navigation provides touch-friendly access
- Tables/forms adapt to mobile screens
- No horizontal overflow at any breakpoint
- Tap targets meet WCAG 44px minimum
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings-photos-polish/12-13-SUMMARY.md`
</output>
