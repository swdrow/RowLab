---
phase: 12-settings-photos-polish
plan: 10
type: execute
wave: 3
depends_on: ["12-01"]
files_modified:
  - src/v2/features/athletes/components/AthletesTableSkeleton.tsx
  - src/v2/features/erg/components/ErgTableSkeleton.tsx
  - src/v2/features/lineup/components/LineupListSkeleton.tsx
  - src/v2/features/seat-racing/components/SessionListSkeleton.tsx
  - src/v2/features/training/components/CalendarSkeleton.tsx
  - src/v2/features/regatta/components/RegattaListSkeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Every data-fetching component has a matching skeleton loader"
    - "Skeletons match the shape of loaded content"
    - "Skeletons use react-loading-skeleton with theme colors"
    - "Athletes, Erg, Lineup, Seat Racing, Training, Regatta all have skeletons"
  artifacts:
    - path: "src/v2/features/athletes/components/AthletesTableSkeleton.tsx"
      provides: "Athletes table loading skeleton"
      exports: ["AthletesTableSkeleton"]
    - path: "src/v2/features/erg/components/ErgTableSkeleton.tsx"
      provides: "Erg tests table loading skeleton"
      exports: ["ErgTableSkeleton"]
  key_links:
    - from: "all skeleton components"
      to: "LoadingSkeleton from common"
      via: "import"
      pattern: "import.*LoadingSkeleton.*from.*common"
---

<objective>
Create skeleton loaders for all data-fetching components across V2 features.

Purpose: Deliver POLISH-04 (skeleton loaders for all data-fetching components).
Output: Feature-specific skeleton components that match content shape.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-settings-photos-polish/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Athletes and Erg skeleton loaders</name>
  <files>
    src/v2/features/athletes/components/AthletesTableSkeleton.tsx
    src/v2/features/athletes/components/AthleteCardSkeleton.tsx
    src/v2/features/erg/components/ErgTableSkeleton.tsx
  </files>
  <action>
Create skeleton components that match the shape of actual content.

**AthletesTableSkeleton.tsx:**
```typescript
import { LoadingSkeleton, SkeletonLine, SkeletonCircle } from '@v2/components/common/LoadingSkeleton';

interface AthletesTableSkeletonProps {
  rows?: number;
}

export const AthletesTableSkeleton: React.FC<AthletesTableSkeletonProps> = ({ rows = 10 }) => (
  <LoadingSkeleton>
    <div className="space-y-0">
      {/* Header row */}
      <div className="flex items-center gap-4 p-4 border-b border-bdr-subtle bg-surface-elevated">
        <SkeletonLine width={24} height={20} />
        <SkeletonLine width="20%" height={20} />
        <SkeletonLine width="15%" height={20} />
        <SkeletonLine width="10%" height={20} />
        <SkeletonLine width="10%" height={20} />
      </div>
      {/* Data rows */}
      {Array.from({ length: rows }).map((_, i) => (
        <div key={i} className="flex items-center gap-4 p-4 border-b border-bdr-subtle">
          <SkeletonCircle size={40} />
          <div className="flex-1 space-y-2">
            <SkeletonLine width="60%" height={18} />
            <SkeletonLine width="40%" height={14} />
          </div>
          <SkeletonLine width={60} height={24} />
          <SkeletonLine width={40} height={24} />
          <SkeletonLine width={80} height={28} />
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);
```

**AthleteCardSkeleton.tsx:**
For grid view:
```typescript
export const AthleteCardSkeleton: React.FC = () => (
  <LoadingSkeleton>
    <div className="p-4 rounded-xl border border-bdr-subtle bg-surface">
      <div className="flex items-center gap-4 mb-4">
        <SkeletonCircle size={64} />
        <div className="flex-1 space-y-2">
          <SkeletonLine width="70%" height={20} />
          <SkeletonLine width="50%" height={16} />
        </div>
      </div>
      <div className="space-y-2">
        <SkeletonLine width="100%" height={12} />
        <SkeletonLine width="80%" height={12} />
      </div>
    </div>
  </LoadingSkeleton>
);

export const AthletesGridSkeleton: React.FC<{ count?: number }> = ({ count = 12 }) => (
  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
    {Array.from({ length: count }).map((_, i) => (
      <AthleteCardSkeleton key={i} />
    ))}
  </div>
);
```

**ErgTableSkeleton.tsx:**
```typescript
export const ErgTableSkeleton: React.FC<{ rows?: number }> = ({ rows = 10 }) => (
  <LoadingSkeleton>
    <div className="overflow-hidden rounded-xl border border-bdr-subtle">
      {/* Header */}
      <div className="flex items-center gap-4 p-3 bg-surface-elevated border-b border-bdr-subtle">
        <SkeletonLine width="15%" height={18} />
        <SkeletonLine width="12%" height={18} />
        <SkeletonLine width="10%" height={18} />
        <SkeletonLine width="10%" height={18} />
        <SkeletonLine width="10%" height={18} />
        <SkeletonLine width="8%" height={18} />
      </div>
      {/* Rows */}
      {Array.from({ length: rows }).map((_, i) => (
        <div key={i} className="flex items-center gap-4 p-3 border-b border-bdr-subtle last:border-0">
          <div className="w-[15%] flex items-center gap-2">
            <SkeletonCircle size={32} />
            <SkeletonLine width={80} height={16} />
          </div>
          <SkeletonLine width="12%" height={16} />
          <SkeletonLine width="10%" height={16} />
          <SkeletonLine width="10%" height={16} />
          <SkeletonLine width="10%" height={16} />
          <SkeletonLine width={60} height={24} />
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);
```
  </action>
  <verify>Skeletons render with shimmer animation and match table/grid layout</verify>
  <done>Athletes and Erg skeletons created matching content shape</done>
</task>

<task type="auto">
  <name>Task 2: Create Lineup and Seat Racing skeletons</name>
  <files>
    src/v2/features/lineup/components/LineupListSkeleton.tsx
    src/v2/features/seat-racing/components/SessionListSkeleton.tsx
    src/v2/features/seat-racing/components/RankingsTableSkeleton.tsx
  </files>
  <action>
**LineupListSkeleton.tsx:**
```typescript
export const LineupListSkeleton: React.FC<{ count?: number }> = ({ count = 5 }) => (
  <LoadingSkeleton>
    <div className="space-y-4">
      {Array.from({ length: count }).map((_, i) => (
        <div key={i} className="p-4 rounded-xl border border-bdr-subtle bg-surface">
          <div className="flex items-center justify-between mb-3">
            <div className="flex items-center gap-3">
              <SkeletonLine width={100} height={24} />
              <SkeletonLine width={60} height={20} />
            </div>
            <SkeletonLine width={80} height={16} />
          </div>
          {/* Boat seats preview */}
          <div className="flex items-center gap-2">
            {Array.from({ length: 8 }).map((_, j) => (
              <SkeletonCircle key={j} size={32} />
            ))}
          </div>
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);

export const LineupBuilderSkeleton: React.FC = () => (
  <LoadingSkeleton>
    <div className="grid grid-cols-12 gap-6">
      {/* Boat area */}
      <div className="col-span-8 p-6 rounded-xl border border-bdr-subtle bg-surface">
        <SkeletonLine width="30%" height={28} className="mb-4" />
        <div className="space-y-3">
          {Array.from({ length: 9 }).map((_, i) => (
            <div key={i} className="flex items-center gap-4 p-3 rounded-lg border border-bdr-subtle">
              <SkeletonLine width={40} height={24} />
              <SkeletonCircle size={40} />
              <SkeletonLine width="60%" height={18} />
            </div>
          ))}
        </div>
      </div>
      {/* Athlete bank */}
      <div className="col-span-4 p-4 rounded-xl border border-bdr-subtle bg-surface">
        <SkeletonLine width="50%" height={20} className="mb-4" />
        <div className="space-y-2">
          {Array.from({ length: 10 }).map((_, i) => (
            <div key={i} className="flex items-center gap-3 p-2">
              <SkeletonCircle size={36} />
              <SkeletonLine width="70%" height={16} />
            </div>
          ))}
        </div>
      </div>
    </div>
  </LoadingSkeleton>
);
```

**SessionListSkeleton.tsx:**
```typescript
export const SessionListSkeleton: React.FC<{ count?: number }> = ({ count = 5 }) => (
  <LoadingSkeleton>
    <div className="space-y-4">
      {Array.from({ length: count }).map((_, i) => (
        <div key={i} className="p-4 rounded-xl border border-bdr-subtle bg-surface">
          <div className="flex items-center justify-between mb-2">
            <SkeletonLine width={150} height={20} />
            <SkeletonLine width={80} height={24} />
          </div>
          <div className="flex items-center gap-4">
            <SkeletonLine width={100} height={16} />
            <SkeletonLine width={60} height={16} />
            <SkeletonLine width={80} height={16} />
          </div>
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);
```

**RankingsTableSkeleton.tsx:**
```typescript
export const RankingsTableSkeleton: React.FC<{ rows?: number }> = ({ rows = 10 }) => (
  <LoadingSkeleton>
    <div className="overflow-hidden rounded-xl border border-bdr-subtle">
      <div className="flex items-center gap-4 p-3 bg-surface-elevated border-b border-bdr-subtle">
        <SkeletonLine width={40} height={18} />
        <SkeletonLine width="30%" height={18} />
        <SkeletonLine width="15%" height={18} />
        <SkeletonLine width="15%" height={18} />
        <SkeletonLine width="15%" height={18} />
      </div>
      {Array.from({ length: rows }).map((_, i) => (
        <div key={i} className="flex items-center gap-4 p-3 border-b border-bdr-subtle last:border-0">
          <SkeletonLine width={30} height={24} />
          <div className="flex items-center gap-2 w-[30%]">
            <SkeletonCircle size={32} />
            <SkeletonLine width={100} height={16} />
          </div>
          <SkeletonLine width="15%" height={24} />
          <SkeletonLine width="15%" height={16} />
          <SkeletonLine width="15%" height={24} />
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);
```
  </action>
  <verify>Lineup and seat racing skeletons match actual UI structure</verify>
  <done>Lineup and Seat Racing skeletons created</done>
</task>

<task type="auto">
  <name>Task 3: Create Training and Regatta skeletons</name>
  <files>
    src/v2/features/training/components/CalendarSkeleton.tsx
    src/v2/features/regatta/components/RegattaListSkeleton.tsx
    src/v2/features/race-day/components/RaceDaySkeleton.tsx
  </files>
  <action>
**CalendarSkeleton.tsx:**
```typescript
export const CalendarSkeleton: React.FC = () => (
  <LoadingSkeleton>
    <div className="p-4 rounded-xl border border-bdr-subtle bg-surface">
      {/* Calendar header */}
      <div className="flex items-center justify-between mb-4">
        <SkeletonLine width={150} height={28} />
        <div className="flex items-center gap-2">
          <SkeletonLine width={40} height={32} />
          <SkeletonLine width={40} height={32} />
        </div>
      </div>
      {/* Day headers */}
      <div className="grid grid-cols-7 gap-2 mb-2">
        {['S', 'M', 'T', 'W', 'T', 'F', 'S'].map((_, i) => (
          <SkeletonLine key={i} width="100%" height={24} />
        ))}
      </div>
      {/* Calendar grid */}
      {Array.from({ length: 5 }).map((_, week) => (
        <div key={week} className="grid grid-cols-7 gap-2 mb-2">
          {Array.from({ length: 7 }).map((_, day) => (
            <div key={day} className="aspect-square p-2 rounded-lg border border-bdr-subtle">
              <SkeletonLine width={20} height={16} className="mb-1" />
              <SkeletonLine width="80%" height={12} />
            </div>
          ))}
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);

export const WeekViewSkeleton: React.FC = () => (
  <LoadingSkeleton>
    <div className="grid grid-cols-7 gap-4">
      {Array.from({ length: 7 }).map((_, i) => (
        <div key={i} className="p-3 rounded-xl border border-bdr-subtle bg-surface min-h-[400px]">
          <SkeletonLine width={60} height={20} className="mb-3" />
          <div className="space-y-2">
            {Array.from({ length: 3 }).map((_, j) => (
              <div key={j} className="p-2 rounded-lg bg-surface-elevated">
                <SkeletonLine width="90%" height={14} />
                <SkeletonLine width="60%" height={12} className="mt-1" />
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);
```

**RegattaListSkeleton.tsx:**
```typescript
export const RegattaListSkeleton: React.FC<{ count?: number }> = ({ count = 5 }) => (
  <LoadingSkeleton>
    <div className="space-y-4">
      {Array.from({ length: count }).map((_, i) => (
        <div key={i} className="p-4 rounded-xl border border-bdr-subtle bg-surface">
          <div className="flex items-center justify-between mb-3">
            <div className="space-y-2">
              <SkeletonLine width={200} height={22} />
              <SkeletonLine width={150} height={16} />
            </div>
            <SkeletonLine width={80} height={28} />
          </div>
          <div className="flex items-center gap-4">
            <SkeletonLine width={100} height={14} />
            <SkeletonLine width={80} height={14} />
            <SkeletonLine width={60} height={14} />
          </div>
        </div>
      ))}
    </div>
  </LoadingSkeleton>
);
```

**RaceDaySkeleton.tsx:**
```typescript
export const RaceDaySkeleton: React.FC = () => (
  <LoadingSkeleton>
    <div className="space-y-6">
      {/* Header */}
      <div className="p-6 rounded-xl border border-bdr-subtle bg-surface">
        <SkeletonLine width="40%" height={32} className="mb-2" />
        <SkeletonLine width="60%" height={20} />
      </div>
      {/* Countdown */}
      <div className="p-6 rounded-xl border border-bdr-subtle bg-surface">
        <SkeletonLine width={150} height={20} className="mb-4" />
        <div className="flex items-center gap-4">
          <SkeletonLine width={80} height={48} />
          <SkeletonLine width={80} height={48} />
          <SkeletonLine width={80} height={48} />
        </div>
      </div>
      {/* Race list */}
      <div className="space-y-3">
        {Array.from({ length: 4 }).map((_, i) => (
          <div key={i} className="p-4 rounded-xl border border-bdr-subtle bg-surface flex items-center gap-4">
            <SkeletonLine width={60} height={24} />
            <div className="flex-1">
              <SkeletonLine width="50%" height={18} />
              <SkeletonLine width="30%" height={14} className="mt-1" />
            </div>
            <SkeletonLine width={80} height={28} />
          </div>
        ))}
      </div>
    </div>
  </LoadingSkeleton>
);
```
  </action>
  <verify>Training and Regatta skeletons render correctly</verify>
  <done>All feature area skeletons created</done>
</task>

</tasks>

<verification>
1. Each feature area has skeleton components
2. Skeletons match the structure of actual loaded content
3. Shimmer animation plays on all skeletons
4. Skeleton colors use theme tokens (work in dark/light/field)
5. No layout shift when content loads (skeleton matches content size)
</verification>

<success_criteria>
- POLISH-04: Skeleton loaders exist for all data-fetching components
- Athletes table/grid have skeletons
- Erg tests table has skeleton
- Lineup list and builder have skeletons
- Seat racing sessions and rankings have skeletons
- Training calendar has skeleton
- Regatta list and race day have skeletons
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings-photos-polish/12-10-SUMMARY.md`
</output>
