---
phase: 15-feature-toggles-recruiting
plan: 10
type: execute
wave: 5
depends_on: ["15-02", "15-03", "15-05", "15-08", "15-09"]
files_modified:
  - src/v2/components/layout/WorkspaceSidebar.tsx
  - src/App.jsx
autonomous: false

must_haves:
  truths:
    - "Coach can toggle advanced features on/off in settings"
    - "UI adapts to show only enabled features"
    - "Coach can create recruit visit events with host assignment"
    - "Host athlete sees assigned visits in their dashboard"
    - "Visit schedules can be uploaded or created inline"
---

<objective>
Integration verification checkpoint for Phase 15 complete functionality.

Purpose: Verify all Phase 15 features work together correctly: feature toggles control navigation, recruit visits integrate with calendar and dashboard, notifications display throughout the app.

Output: Final integration fixes if needed, human verification of complete functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-feature-toggles-recruiting/15-CONTEXT.md
All prior plan SUMMARYs for this phase
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify navigation integration with feature toggles</name>
  <files>src/v2/components/layout/WorkspaceSidebar.tsx</files>
  <action>
Verify that feature toggle integration in navigation is complete:

1. Check WorkspaceSidebar.tsx includes feature filtering logic from Plan 15-03
2. Ensure "Recruiting" nav item only shows when 'recruiting' feature is enabled
3. Ensure all advanced feature nav items are properly gated

If not already integrated:
- Add "Recruiting" nav item to appropriate context (Coach context)
- Gate it with 'recruiting' feature ID
- Add icon (Users from lucide-react)
- Path: '/app/recruiting'

Test by:
1. Disabling 'recruiting' feature in settings → nav item should disappear
2. Enabling 'recruiting' feature → nav item should appear
3. Direct URL access when disabled → should show FeatureDiscoveryHint
  </action>
  <verify>Toggle recruiting feature off/on - navigation updates immediately</verify>
  <done>Recruiting nav item gated by feature toggle, immediate UI response to toggle changes</done>
</task>

<task type="auto">
  <name>Task 2: Verify route registration complete</name>
  <files>src/App.jsx</files>
  <action>
Ensure all Phase 15 routes are registered:

1. Check /app/recruiting route exists pointing to RecruitingPage
2. Check /visit/:token public route exists for shared visit links
3. Verify routes are within proper authentication/layout wrappers

If missing, add routes following existing patterns in App.jsx.

Also verify:
- ToastProvider is added to app root (from Plan 15-09)
- Feature stores are properly initialized
  </action>
  <verify>Navigate to /app/recruiting - page renders; /visit/:token shows public view</verify>
  <done>All routes registered, ToastProvider in place, stores initialized</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Phase 15 functionality</name>
  <what-built>
Complete Phase 15: Feature Toggles & Recruiting foundation including:
- Feature toggle system with Core (always on) and Advanced (toggleable) groups
- Features settings tab with toggle UI
- Feature-gated navigation (disabled features hidden)
- Recruit visit CRUD (create, read, update, delete)
- Visit schedule editor (rich text via Lexical or PDF upload)
- Host athlete dashboard widget showing assigned visits
- Recruiting page with visit list, filtering, and detail panel
- Notification preferences with channels, quiet hours, and per-feature toggles
- Sonner toast notifications throughout the app
  </what-built>
  <how-to-verify>
**Feature Toggles:**
1. Navigate to Settings → Features tab
2. Verify Core features show "Always on" badges (no toggle)
3. Verify Advanced features have working toggles
4. Toggle "Recruiting" OFF → verify "Recruiting" nav item disappears
5. Toggle "Recruiting" ON → verify nav item reappears
6. Refresh page → verify toggle state persists

**Recruit Visit Creation:**
1. Navigate to /app/recruiting
2. Click "New Visit" button
3. Fill in required fields (name, date, times)
4. Select a host athlete from dropdown
5. Add rich text schedule OR upload PDF
6. Submit form → verify visit appears in list

**Visit Detail Panel:**
1. Click on a visit card
2. Verify slide-out panel shows all visit details
3. Test share button → copies link
4. Test edit button → opens edit form
5. Test delete with confirmation

**Host Athlete Dashboard:**
1. If logged in as athlete who is assigned as host:
   - Verify "Your Upcoming Hosting Duties" widget appears on dashboard
   - Verify it shows the assigned visit

**Notification Preferences:**
1. Navigate to Settings → Notifications tab
2. Toggle channel master switches (In-App, Email, Push)
3. Configure quiet hours with start/end times
4. Toggle individual notification types
5. Refresh → verify preferences persist

**Toast Notifications:**
1. Create a recruit visit → verify success toast appears
2. Delete a visit → verify toast notification
3. Any error → verify error toast with message

  </how-to-verify>
  <resume-signal>Type "approved" if all verification passes, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Complete Phase 15 success criteria verification:
1. [ ] Coach can toggle advanced features on/off in settings
2. [ ] UI adapts to show only enabled features (navigation hides disabled)
3. [ ] Coach can create recruit visit events with host assignment
4. [ ] Host athlete sees assigned visits in their dashboard
5. [ ] Visit schedules can be uploaded (PDF) or created inline (rich text)
6. [ ] Notification preferences work with channels, quiet hours, per-feature toggles
7. [ ] Toast notifications display throughout the app
</verification>

<success_criteria>
- [ ] Feature toggles persist to localStorage and survive refresh
- [ ] Navigation immediately reflects feature toggle changes
- [ ] Recruit visit CRUD operations work end-to-end
- [ ] PDF upload stores file and returns URL
- [ ] Rich text editor produces sanitized HTML
- [ ] Host athlete widget shows assigned visits
- [ ] Share token generation and public access work
- [ ] All notification preference controls functional
- [ ] Sonner toasts display with V2 styling
</success_criteria>

<output>
After verification approval, create `.planning/phases/15-feature-toggles-recruiting/15-10-SUMMARY.md` with final integration notes.
</output>
