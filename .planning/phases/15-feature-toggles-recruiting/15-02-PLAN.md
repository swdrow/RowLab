---
phase: 15-feature-toggles-recruiting
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/v2/types/settings.ts
  - src/v2/features/settings/components/SettingsTabs.tsx
  - src/v2/features/settings/components/FeaturesSection.tsx
  - src/v2/features/settings/components/FeatureGroupCard.tsx
  - src/v2/features/settings/components/FeatureToggleRow.tsx
  - src/v2/features/settings/pages/SettingsPage.tsx
  - src/v2/features/settings/components/index.ts
autonomous: true

must_haves:
  truths:
    - "Coach can access Features tab in settings"
    - "Features are grouped into Core and Advanced sections"
    - "Coach can toggle Advanced features on/off"
    - "Toggle state persists after leaving settings"
  artifacts:
    - path: "src/v2/features/settings/components/FeaturesSection.tsx"
      provides: "Main features settings section"
      min_lines: 50
    - path: "src/v2/features/settings/components/FeatureGroupCard.tsx"
      provides: "Card component for each feature group"
      min_lines: 40
    - path: "src/v2/features/settings/components/FeatureToggleRow.tsx"
      provides: "Individual feature toggle row"
      min_lines: 30
  key_links:
    - from: "src/v2/features/settings/components/FeaturesSection.tsx"
      to: "useFeaturePreferenceStore"
      via: "Zustand store import"
      pattern: "useFeaturePreferenceStore"
    - from: "src/v2/features/settings/pages/SettingsPage.tsx"
      to: "FeaturesSection"
      via: "tab conditional render"
      pattern: "activeTab.*features.*FeaturesSection"
---

<objective>
Create the Features settings tab UI where coaches can toggle advanced features on/off.

Purpose: Coaches need a way to customize which advanced features their team uses. This UI provides clear visual distinction between Core (always-on) and Advanced (toggleable) features.

Output: New "Features" tab in settings with grouped toggles for all features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-feature-toggles-recruiting/15-CONTEXT.md
@.planning/phases/15-feature-toggles-recruiting/15-01-PLAN.md

# Existing settings patterns
@src/v2/features/settings/pages/SettingsPage.tsx
@src/v2/features/settings/components/SettingsTabs.tsx
@src/v2/features/settings/components/PreferencesSection.tsx
@src/v2/types/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'features' tab type and update SettingsTabs</name>
  <files>src/v2/types/settings.ts, src/v2/features/settings/components/SettingsTabs.tsx</files>
  <action>
1. In src/v2/types/settings.ts:
   - Update `SettingsTab` type to include 'features':
     `export type SettingsTab = 'profile' | 'preferences' | 'security' | 'integrations' | 'features' | 'team' | 'billing';`

2. In src/v2/features/settings/components/SettingsTabs.tsx:
   - Import `ToggleLeft` from 'lucide-react' (for features icon)
   - Add features tab to TABS array BEFORE 'team':
     `{ id: 'features', label: 'Features', icon: ToggleLeft }`
   - This tab should be visible to all users (not ownerOnly), but only owners/admins can actually toggle settings (handle in component)
  </action>
  <verify>SettingsTabs renders with new "Features" tab visible</verify>
  <done>SettingsTab type updated, Features tab appears in settings navigation between Integrations and Team</done>
</task>

<task type="auto">
  <name>Task 2: Create FeatureToggleRow and FeatureGroupCard components</name>
  <files>src/v2/features/settings/components/FeatureToggleRow.tsx, src/v2/features/settings/components/FeatureGroupCard.tsx</files>
  <action>
1. Create FeatureToggleRow.tsx:
   - Props: { feature: FeatureConfig, enabled: boolean, onToggle: () => void, disabled?: boolean }
   - Display: Feature name (bold), description (muted text), toggle switch on right
   - If feature.group === 'core', show "Always on" badge instead of toggle
   - Use existing Toggle component pattern from V2 (or simple styled checkbox toggle)
   - Add transition animation on toggle (opacity change, subtle scale)
   - disabled prop prevents toggle interaction (for non-admin users)

2. Create FeatureGroupCard.tsx:
   - Props: { group: FeatureGroup, features: (FeatureConfig & { enabled: boolean })[], onToggle: (featureId: FeatureId) => void, canEdit: boolean }
   - Display card with:
     - Header: group.name (large), group.description (muted)
     - If group.id === 'core': Show info banner "Core features are always enabled"
     - If group.id === 'advanced': Show info banner "Enable features your team needs"
     - List of FeatureToggleRow for each feature
   - Use existing card styling patterns from V2 settings (bg-surface-elevated, rounded-xl, border)
   - canEdit=false disables all toggles (for non-owner/admin viewing)

Follow existing V2 styling patterns:
- bg-surface-elevated for cards
- text-txt-primary, text-txt-secondary for typography
- border-bdr for borders
- Spring animations with SPRING_CONFIG
  </action>
  <verify>Components render without errors when imported</verify>
  <done>FeatureToggleRow shows toggle or "Always on" badge, FeatureGroupCard displays group header with info banner and feature list</done>
</task>

<task type="auto">
  <name>Task 3: Create FeaturesSection and integrate into SettingsPage</name>
  <files>src/v2/features/settings/components/FeaturesSection.tsx, src/v2/features/settings/pages/SettingsPage.tsx, src/v2/features/settings/components/index.ts</files>
  <action>
1. Create FeaturesSection.tsx:
   - Import useFeaturePreferenceStore from '@v2/stores/featurePreferenceStore'
   - Import FEATURE_GROUPS, ALL_FEATURES from '@v2/types/feature-toggles'
   - Import useAuthStore to check activeTeamRole

   - Determine canEdit: true if activeTeamRole is 'OWNER' or 'ADMIN'

   - Get enabledFeatures and toggleFeature from store

   - Map features with enabled state:
     ```tsx
     const featuresWithState = ALL_FEATURES.map(f => ({
       ...f,
       enabled: store.isFeatureEnabled(f.id)
     }));
     ```

   - Render two FeatureGroupCard components (one for 'core', one for 'advanced')

   - If !canEdit, show info banner at top: "Only team owners and admins can change feature settings"

2. Update src/v2/features/settings/pages/SettingsPage.tsx:
   - Import FeaturesSection
   - Add 'features' to validTabs array
   - Add conditional render for features tab:
     ```tsx
     {activeTab === 'features' && (
       <FeaturesSection />
     )}
     ```

3. Update src/v2/features/settings/components/index.ts:
   - Export FeaturesSection, FeatureGroupCard, FeatureToggleRow
  </action>
  <verify>Navigate to /app/settings?tab=features - Features tab renders with Core and Advanced sections</verify>
  <done>FeaturesSection displays both feature groups, toggles work for Advanced features, settings persist to localStorage</done>
</task>

</tasks>

<verification>
1. Navigate to Settings → Features tab is visible
2. Core features section shows "Always on" badges, not toggles
3. Advanced features section shows toggles
4. Toggle an advanced feature → refresh page → toggle state persists
5. Non-owner/admin users see features but cannot toggle (disabled state)
</verification>

<success_criteria>
- [ ] "Features" tab appears in settings navigation
- [ ] Core features display as "Always on" (no toggle)
- [ ] Advanced features have working toggles
- [ ] Toggle state persists in localStorage
- [ ] Only OWNER/ADMIN can toggle (others see disabled state)
- [ ] UI follows V2 design system (surfaces, colors, typography)
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-toggles-recruiting/15-02-SUMMARY.md`
</output>
