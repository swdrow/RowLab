---
phase: 17-design-overhaul
plan: 21
type: execute
wave: 2
depends_on: ["17-20"]
files_modified:
  - src/v2/components/landing/FeaturePreview.tsx
  - src/v2/components/landing/LineupPreview.tsx
  - src/pages/LandingPage.tsx
autonomous: true

must_haves:
  truths:
    - "Feature tiles have visual previews, not just text"
    - "Lineup Builder tile shows drag-drop preview or boat diagram"
    - "Previews are lightweight (no heavy images)"
    - "Animations are subtle and performant"
  artifacts:
    - path: "src/v2/components/landing/FeaturePreview.tsx"
      provides: "Reusable preview wrapper component"
      contains: "FeaturePreview"
    - path: "src/v2/components/landing/LineupPreview.tsx"
      provides: "Lineup builder visual preview"
      contains: "boat|seat"
---

<objective>
Add visual previews to feature tiles to make the bento grid compelling.

Purpose: Feature tiles with just text are boring. Each tile needs a lightweight visual preview that hints at the feature's functionality. This makes the landing page visually interesting without heavy images.

Output: Feature preview components integrated into bento tiles
</objective>

<context>
@.planning/phases/17-design-overhaul/17-LANDING-RESEARCH.md
@src/v2/components/landing/BentoTile.tsx
@src/pages/LandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LineupPreview component</name>
  <files>src/v2/components/landing/LineupPreview.tsx</files>
  <action>
Create a lightweight SVG-based preview of the lineup builder:

```tsx
import React from 'react';

/**
 * LineupPreview - Visual preview of lineup builder for landing page
 *
 * Shows a simplified 4+ boat diagram with seat slots and an athlete
 * being "dragged" into position. Uses SVG for crisp rendering.
 */
export const LineupPreview: React.FC = () => {
  return (
    <div className="relative w-full h-32 bg-ink-base/50 rounded-lg overflow-hidden">
      {/* Boat outline - simplified 4+ shell */}
      <svg
        viewBox="0 0 300 60"
        className="absolute inset-0 w-full h-full p-4"
        fill="none"
        stroke="currentColor"
        strokeWidth="1.5"
      >
        {/* Hull */}
        <path
          d="M10 30 Q30 10, 150 10 Q270 10, 290 30 Q270 50, 150 50 Q30 50, 10 30"
          className="stroke-ink-border fill-ink-raised/50"
        />

        {/* Seat slots */}
        <g className="fill-ink-base stroke-ink-border">
          {/* Bow */}
          <rect x="50" y="20" width="30" height="20" rx="3" />
          {/* 2 seat */}
          <rect x="90" y="20" width="30" height="20" rx="3" />
          {/* 3 seat */}
          <rect x="130" y="20" width="30" height="20" rx="3" className="fill-ink-bright/20 stroke-ink-primary" />
          {/* Stroke */}
          <rect x="170" y="20" width="30" height="20" rx="3" />
          {/* Cox */}
          <circle cx="230" cy="30" r="12" />
        </g>

        {/* "Dragged" athlete indicator */}
        <g className="animate-pulse">
          <rect
            x="130" y="20" width="30" height="20" rx="3"
            className="fill-ink-bright/30 stroke-ink-bright"
            strokeDasharray="4 2"
          />
        </g>

        {/* Seat numbers */}
        <g className="fill-ink-secondary text-xs" fontSize="8">
          <text x="65" y="34" textAnchor="middle">B</text>
          <text x="105" y="34" textAnchor="middle">2</text>
          <text x="145" y="34" textAnchor="middle">3</text>
          <text x="185" y="34" textAnchor="middle">S</text>
          <text x="230" y="34" textAnchor="middle">C</text>
        </g>
      </svg>

      {/* Floating "athlete card" being dragged */}
      <div
        className="absolute top-2 right-4 px-2 py-1 bg-ink-bright text-ink-deep text-xs font-medium rounded shadow-lg"
        style={{ transform: 'rotate(-3deg)' }}
      >
        J. Smith
      </div>
    </div>
  );
};

export default LineupPreview;
```
  </action>
  <verify>
File exists at src/v2/components/landing/LineupPreview.tsx
grep "boat\|seat" src/v2/components/landing/LineupPreview.tsx returns match
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create MetricPreview component</name>
  <files>src/v2/components/landing/MetricPreview.tsx</files>
  <action>
Create a preview showing chromatic data metrics:

```tsx
import React from 'react';

/**
 * MetricPreview - Shows sample performance metrics
 *
 * Demonstrates the "chromatic data" principle - metrics
 * have color while the surrounding UI is monochrome.
 */
export const MetricPreview: React.FC = () => {
  return (
    <div className="flex items-end justify-between gap-4 h-24 px-2">
      {/* Metric 1 - Erg split */}
      <div className="flex flex-col items-center">
        <span className="font-mono text-2xl font-semibold text-data-good tabular-nums">
          1:42.3
        </span>
        <span className="text-xs text-ink-secondary uppercase tracking-wide">
          Split
        </span>
      </div>

      {/* Metric 2 - Distance */}
      <div className="flex flex-col items-center">
        <span className="font-mono text-2xl font-semibold text-data-excellent tabular-nums">
          6,247
        </span>
        <span className="text-xs text-ink-secondary uppercase tracking-wide">
          Meters
        </span>
      </div>

      {/* Metric 3 - Stroke rate */}
      <div className="flex flex-col items-center">
        <span className="font-mono text-2xl font-semibold text-ink-primary tabular-nums">
          28
        </span>
        <span className="text-xs text-ink-secondary uppercase tracking-wide">
          S/M
        </span>
      </div>
    </div>
  );
};

export default MetricPreview;
```
  </action>
  <verify>
File exists at src/v2/components/landing/MetricPreview.tsx
grep "data-good\|data-excellent" src/v2/components/landing/MetricPreview.tsx returns match
  </verify>
</task>

<task type="auto">
  <name>Task 3: Create CalendarPreview component</name>
  <files>src/v2/components/landing/CalendarPreview.tsx</files>
  <action>
Create a mini calendar preview for training:

```tsx
import React from 'react';

/**
 * CalendarPreview - Mini training calendar
 *
 * Shows a week view with some sessions marked.
 */
export const CalendarPreview: React.FC = () => {
  const days = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
  const hasSession = [true, false, true, true, false, true, false];

  return (
    <div className="flex justify-between gap-1 h-16">
      {days.map((day, i) => (
        <div key={i} className="flex flex-col items-center gap-1 flex-1">
          <span className="text-xs text-ink-secondary">{day}</span>
          <div
            className={`
              w-full h-10 rounded
              ${hasSession[i]
                ? 'bg-ink-primary/20 border border-ink-primary/40'
                : 'bg-ink-base border border-ink-border/50'
              }
            `}
          />
        </div>
      ))}
    </div>
  );
};

export default CalendarPreview;
```
  </action>
  <verify>
File exists at src/v2/components/landing/CalendarPreview.tsx
  </verify>
</task>

<task type="auto">
  <name>Task 4: Create RankingPreview component</name>
  <files>src/v2/components/landing/RankingPreview.tsx</files>
  <action>
Create a mini rankings table preview:

```tsx
import React from 'react';

/**
 * RankingPreview - Mini athlete rankings
 *
 * Shows a condensed view of Bradley-Terry style rankings.
 */
export const RankingPreview: React.FC = () => {
  const athletes = [
    { rank: 1, name: 'A. Johnson', rating: 1842 },
    { rank: 2, name: 'M. Chen', rating: 1798 },
    { rank: 3, name: 'K. Williams', rating: 1756 },
  ];

  return (
    <div className="space-y-2">
      {athletes.map((a) => (
        <div
          key={a.rank}
          className="flex items-center justify-between px-2 py-1.5 bg-ink-base/50 rounded"
        >
          <div className="flex items-center gap-2">
            <span className="text-xs font-mono text-ink-secondary w-4">
              {a.rank}
            </span>
            <span className="text-sm text-ink-primary truncate">
              {a.name}
            </span>
          </div>
          <span className="text-xs font-mono text-data-good tabular-nums">
            {a.rating}
          </span>
        </div>
      ))}
    </div>
  );
};

export default RankingPreview;
```
  </action>
  <verify>
File exists at src/v2/components/landing/RankingPreview.tsx
  </verify>
</task>

<task type="auto">
  <name>Task 5: Update landing components index</name>
  <files>src/v2/components/landing/index.ts</files>
  <action>
Add new preview components to exports:

```ts
export { BentoGrid, GRID_AREAS } from './BentoGrid';
export { BentoTile } from './BentoTile';
export { LineupPreview } from './LineupPreview';
export { MetricPreview } from './MetricPreview';
export { CalendarPreview } from './CalendarPreview';
export { RankingPreview } from './RankingPreview';
```
  </action>
  <verify>
grep "LineupPreview" src/v2/components/landing/index.ts returns match
grep "MetricPreview" src/v2/components/landing/index.ts returns match
  </verify>
</task>

<task type="auto">
  <name>Task 6: Integrate previews into LandingPage</name>
  <files>src/pages/LandingPage.tsx</files>
  <action>
Update the bento tiles to use preview components:

- Lineup Builder tile: Use LineupPreview
- Erg Data tile: Use MetricPreview
- Training tile: Use CalendarPreview
- Seat Racing tile: Use RankingPreview

Import and add to the appropriate BentoTile `preview` props.
  </action>
  <verify>
grep "LineupPreview" src/pages/LandingPage.tsx returns match
grep "MetricPreview" src/pages/LandingPage.tsx returns match
  </verify>
</task>

</tasks>

<verification>
1. LineupPreview shows boat diagram with drag indicator
2. MetricPreview shows chromatic data metrics
3. CalendarPreview shows mini week view
4. RankingPreview shows mini rankings table
5. All previews integrated into LandingPage bento tiles
6. Previews are lightweight (SVG/CSS, no images)
7. Animations are subtle (no flashy effects)
</verification>

<success_criteria>
- Each major feature tile has a visual preview
- Previews hint at functionality without screenshots
- Chromatic data principle visible in MetricPreview
- All previews render correctly in bento grid
- Mobile responsive (previews scale appropriately)
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-overhaul/17-21-SUMMARY.md`
</output>
