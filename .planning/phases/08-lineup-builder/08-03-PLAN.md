---
phase: 08-lineup-builder
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/v2/components/lineup/SeatWarningBadge.tsx
  - src/v2/components/lineup/SeatSlot.tsx
  - src/v2/components/lineup/DraggableAthleteCard.tsx
  - src/v2/components/lineup/BoatView.tsx
autonomous: true

must_haves:
  truths:
    - "Coach sees warning badge when athlete is in wrong side seat"
    - "Coach sees warning badge when non-coxswain is in coxswain seat"
    - "Warnings do not block assignment - coach can override"
    - "Drag-drop interactions have smooth spring-physics animations"
  artifacts:
    - path: "src/v2/components/lineup/SeatWarningBadge.tsx"
      provides: "Warning badge component for validation issues"
      min_lines: 40
  key_links:
    - from: "src/v2/components/lineup/SeatSlot.tsx"
      to: "src/utils/boatConfig.js"
      via: "validateSeatAssignment function"
      pattern: "validateSeatAssignment"
    - from: "src/v2/components/lineup/SeatSlot.tsx"
      to: "framer-motion"
      via: "motion.div with spring config"
      pattern: "motion\\.div"
---

<objective>
Add validation warnings and spring-physics animations to the lineup builder.

Purpose: Show non-blocking warnings for port/starboard mismatches and coxswain seat violations (LINE-05, DESIGN-05). Coaches see warnings but can always override - "trust the coach" philosophy.

Output: SeatWarningBadge component, validation integration, and Framer Motion spring animations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lineup-builder/08-CONTEXT.md
@.planning/phases/08-lineup-builder/08-RESEARCH.md
@.planning/phases/08-lineup-builder/08-01-SUMMARY.md
@src/utils/boatConfig.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SeatWarningBadge component</name>
  <files>src/v2/components/lineup/SeatWarningBadge.tsx</files>
  <action>
Create warning badge component for seat validation issues:

1. Props: warnings array of { type: 'side' | 'cox', message: string }
2. Display small warning icon badge (yellow/amber background, dark icon)
3. Show tooltip on hover with full warning message(s)
4. Position: top-right corner of seat slot
5. Per CONTEXT.md: "Warnings always visible on seats as badges - constant awareness, no hover required"
6. Badge shows count if multiple warnings (e.g., "2")
7. Use Lucide AlertTriangle icon
8. Styling: rounded-full, amber-500 bg, text-amber-900, small size (w-5 h-5)

Validation types:
- 'side': Port/starboard mismatch (athlete prefers other side)
- 'cox': Non-coxswain in coxswain seat
  </action>
  <verify>Badge renders with icon, tooltip shows on hover with warning messages</verify>
  <done>SeatWarningBadge displays validation warnings visibly without blocking interactions</done>
</task>

<task type="auto">
  <name>Task 2: Integrate validation into SeatSlot</name>
  <files>src/v2/components/lineup/SeatSlot.tsx</files>
  <action>
Add validation checks and warning display to SeatSlot:

1. Import validateSeatAssignment from src/utils/boatConfig.js
2. When seat has athlete, check for warnings:
   ```
   const warnings = [];

   // Port/starboard check
   const sideValidation = validateSeatAssignment(athlete, seat);
   if (sideValidation.warning) {
     warnings.push({ type: 'side', message: sideValidation.warning });
   }

   // Coxswain check (only for coxswain seat)
   if (isCoxswain && athlete && !athlete.isCoxswain) {
     warnings.push({ type: 'cox', message: 'Non-coxswain in coxswain seat' });
   }
   ```
3. Render SeatWarningBadge when warnings.length > 0
4. Position badge in top-right of seat slot (absolute positioning)
5. Per CONTEXT.md:
   - "Port/starboard validation: soft warning - allow drop but show warning badge, coach can override"
   - "Coxswain seat validation: soft warning - non-cox in cox seat shows warning"
   - "Save always allowed regardless of warnings - warnings are informational only"

6. Update drag-over highlight color:
   - Green (#10b981) when athlete matches seat preference
   - Yellow (#f59e0b) when athlete has mismatch warning
   - Always allow drop (never block)
  </action>
  <verify>Warnings show when athlete in wrong side seat or non-cox in cox seat, drops still work</verify>
  <done>SeatSlot shows validation warnings but never blocks assignment</done>
</task>

<task type="auto">
  <name>Task 3: Add Framer Motion spring animations</name>
  <files>src/v2/components/lineup/SeatSlot.tsx, src/v2/components/lineup/DraggableAthleteCard.tsx, src/v2/components/lineup/BoatView.tsx</files>
  <action>
Add spring-physics animations per DESIGN-05:

1. Define shared spring config:
   ```
   const springConfig = {
     type: "spring",
     stiffness: 300,
     damping: 28,
     restDelta: 0.00001,
   };
   ```

2. SeatSlot animations:
   - Wrap in motion.div with layout prop
   - Scale up slightly on drag-over: animate={{ scale: isOver ? 1.02 : 1 }}
   - Border color transition on drag-over
   - WhileHover subtle scale (1.01)
   - Use spring transition

3. DraggableAthleteCard animations:
   - Wrap in motion.div
   - Entry animation: initial={{ opacity: 0, scale: 0.8 }}, animate={{ opacity: 1, scale: 1 }}
   - Exit animation: exit={{ opacity: 0, scale: 0.8 }}
   - WhileDrag scale increase: whileDrag={{ scale: 1.05 }}
   - Use spring transition

4. BoatView animations:
   - AnimatePresence for athlete cards entering/exiting seats
   - Layout animation for seat reordering
   - Stagger children on initial mount (optional enhancement)

Per RESEARCH.md: "Spring physics feel more natural, velocity-aware, don't require precise timing curves"
  </action>
  <verify>Drag interactions feel smooth with spring physics, no jarring transitions</verify>
  <done>All drag-drop interactions have spring-physics animations (DESIGN-05)</done>
</task>

</tasks>

<verification>
1. Warning badge appears when athlete is in wrong-side seat
2. Warning badge appears when non-coxswain is in coxswain seat
3. Warnings do not prevent assignment (coach can always override)
4. Tooltip shows full warning text on hover
5. Seats scale smoothly on drag-over
6. Athlete cards animate in/out with spring physics
7. No jarring or abrupt transitions during drag-drop
</verification>

<success_criteria>
- LINE-05 complete: System validates seat assignments with soft warnings
- DESIGN-05 complete: Spring-physics animations on all drag-drop
- Warnings are informational only, never blocking
- Smooth, natural-feeling animations
</success_criteria>

<output>
After completion, create `.planning/phases/08-lineup-builder/08-03-SUMMARY.md`
</output>
