---
phase: 08-lineup-builder
plan: 07
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/v2/components/lineup/BiometricsPanel.tsx
  - src/v2/utils/biometricsCalculations.ts
  - src/v2/components/lineup/LineupWorkspace.tsx
autonomous: true

must_haves:
  truths:
    - "Coach sees average biometrics as lineup is built"
    - "Biometrics update when athletes are assigned or removed"
    - "Display shows weight, height, and 2k split averages"
  artifacts:
    - path: "src/v2/components/lineup/BiometricsPanel.tsx"
      provides: "Panel displaying live biometrics for current lineup"
      min_lines: 80
    - path: "src/v2/utils/biometricsCalculations.ts"
      provides: "Utility functions for calculating lineup averages"
      min_lines: 50
  key_links:
    - from: "src/v2/components/lineup/BiometricsPanel.tsx"
      to: "src/store/lineupStore.js"
      via: "activeBoats subscription"
      pattern: "useLineupStore.*activeBoats"
---

<objective>
Implement live biometrics display that updates as coaches build lineups.

Purpose: Show average weight, height, and 2k split for athletes in the lineup, helping coaches make informed decisions (LINE-12).

Output: BiometricsPanel component with per-boat and total statistics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lineup-builder/08-CONTEXT.md
@.planning/phases/08-lineup-builder/08-RESEARCH.md
@.planning/phases/08-lineup-builder/08-02-SUMMARY.md
@src/store/lineupStore.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create biometricsCalculations utility</name>
  <files>src/v2/utils/biometricsCalculations.ts</files>
  <action>
Create utility functions for calculating lineup biometrics:

1. `calculateBoatBiometrics(boat)`:
   - Extract all assigned athletes from boat (seats + coxswain)
   - Calculate averages for:
     - Weight (kg) - average, min, max
     - Height (cm) - average, min, max
     - 2k split (seconds) - average, fastest, slowest
   - Return null values for metrics with insufficient data
   - Exclude coxswain from erg averages (they don't row)

2. `calculateTotalBiometrics(boats)`:
   - Aggregate all assigned athletes across all boats
   - Same calculations as per-boat
   - Deduplicate athletes (shouldn't happen but defensive)

3. `formatSplit(seconds)`:
   - Convert seconds to MM:SS.s format
   - Return "--:--.-" if no value

4. `formatWeight(kg)`:
   - Return formatted with unit: "75.5 kg"
   - Return "--" if no value

5. `formatHeight(cm)`:
   - Return formatted with unit: "185 cm"
   - Return "--" if no value

Handle edge cases:
- No athletes assigned: return null
- Athletes missing biometric data: exclude from average
- All athletes missing a metric: return null for that metric

Per RESEARCH.md: "Calculate averages from current lineup"
  </action>
  <verify>Functions return correct averages for test data, handle edge cases</verify>
  <done>biometricsCalculations provides accurate averages with proper edge case handling</done>
</task>

<task type="auto">
  <name>Task 2: Create BiometricsPanel component</name>
  <files>src/v2/components/lineup/BiometricsPanel.tsx</files>
  <action>
Create panel displaying live biometrics for current lineup:

1. Subscribe to activeBoats from useLineupStore
2. Calculate biometrics using utility functions (useMemo for performance)
3. Display sections:
   - Per-boat stats (if multiple boats)
   - Total/combined stats

4. Metrics displayed:
   - Athlete count (X of Y seats filled)
   - Average weight with range (e.g., "78.5 kg (72-85)")
   - Average height with range (e.g., "186 cm (178-195)")
   - Average 2k split (e.g., "6:32.5")

5. Visual design:
   - Compact card/panel layout
   - V2 design tokens
   - Clear labels and values
   - Updates smoothly on changes (no jarring flicker)

6. Empty state:
   - "No athletes assigned" when all boats empty
   - Graceful degradation for missing data

Per CONTEXT.md: "Live average biometrics display"
Per RESEARCH.md: "Separate panel (less cluttered). Update on drag end (not during drag). Show per-boat and total."

Updates on drag end automatically since activeBoats changes trigger re-render.
  </action>
  <verify>Panel shows correct averages, updates when athletes assigned/removed</verify>
  <done>BiometricsPanel displays live averages that update with lineup changes</done>
</task>

<task type="auto">
  <name>Task 3: Integrate BiometricsPanel into LineupWorkspace</name>
  <files>src/v2/components/lineup/LineupWorkspace.tsx</files>
  <action>
Add BiometricsPanel to the lineup builder workspace:

1. Import BiometricsPanel
2. Position in workspace layout:
   - Option A: Below toolbar, above boats (horizontal strip)
   - Option B: In right sidebar (if space allows)
   - Option C: Collapsible panel that can be toggled

   Recommend Option A for simplicity - always visible, doesn't take much space.

3. Layout adjustment:
   ```
   <div className="lineup-workspace">
     <LineupToolbar />
     <BiometricsPanel />
     <div className="workspace-content">
       <AthleteBank />
       <div className="boats-area">
         {activeBoats.map(boat => <BoatView ... />)}
       </div>
     </div>
   </div>
   ```

4. Ensure panel doesn't cause layout shifts when values update

5. Update index.ts exports
  </action>
  <verify>BiometricsPanel visible in workspace, updates live as lineup is built</verify>
  <done>BiometricsPanel integrated into workspace with real-time updates (LINE-12)</done>
</task>

</tasks>

<verification>
1. BiometricsPanel shows in lineup builder workspace
2. Adding athlete to seat updates averages immediately
3. Removing athlete updates averages immediately
4. Swapping athletes updates averages correctly
5. Per-boat and total statistics both shown
6. Missing data handled gracefully (shows "--" not errors)
7. Format is readable (proper units, decimal places)
</verification>

<success_criteria>
- LINE-12 complete: System displays average biometrics as lineup is built
- Averages are accurate and update in real-time
- Clear, readable presentation
- Handles incomplete data gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/08-lineup-builder/08-07-SUMMARY.md`
</output>
