---
phase: 08-lineup-builder
plan: 06
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/v2/utils/lineupPdfExport.ts
  - src/v2/components/lineup/ExportPDFButton.tsx
  - src/v2/components/lineup/PrintableLineup.tsx
  - src/v2/components/lineup/LineupToolbar.tsx
autonomous: true

must_haves:
  truths:
    - "Coach can export lineup as PDF"
    - "PDF is print-ready with high-contrast large font"
    - "PDF includes lineup name, date, and all boat assignments"
  artifacts:
    - path: "src/v2/utils/lineupPdfExport.ts"
      provides: "PDF generation utility using jsPDF + html2canvas"
      min_lines: 80
    - path: "src/v2/components/lineup/ExportPDFButton.tsx"
      provides: "Button component triggering PDF export"
      min_lines: 40
    - path: "src/v2/components/lineup/PrintableLineup.tsx"
      provides: "Print-optimized lineup view for PDF capture"
      min_lines: 100
  key_links:
    - from: "src/v2/utils/lineupPdfExport.ts"
      to: "jspdf"
      via: "PDF generation"
      pattern: "new jsPDF"
    - from: "src/v2/utils/lineupPdfExport.ts"
      to: "html2canvas"
      via: "HTML to canvas rendering"
      pattern: "html2canvas"
---

<objective>
Implement print-ready PDF export for lineups using jsPDF and html2canvas.

Purpose: Enable coaches to export lineups as PDFs suitable for printing on the dock or boathouse (LINE-11). Focus on high-contrast, large font readability.

Output: PDF export utility, PrintableLineup component, and ExportPDFButton integrated into toolbar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lineup-builder/08-CONTEXT.md
@.planning/phases/08-lineup-builder/08-RESEARCH.md
@.planning/phases/08-lineup-builder/08-02-SUMMARY.md
@src/store/lineupStore.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrintableLineup component</name>
  <files>src/v2/components/lineup/PrintableLineup.tsx</files>
  <action>
Create print-optimized lineup view designed for PDF capture:

1. Props: boats array, lineupName, date
2. Design for readability on paper (print at dock/boathouse):
   - White background (#ffffff)
   - Large font sizes: title 24pt, boat names 18pt, seat numbers 14pt, athlete names 16pt
   - High contrast: black text on white
   - Clear borders and separators
   - Generous spacing between boats

3. Layout per boat:
   - Boat header: boat class name, shell name
   - Seats in vertical list (bow at top, stroke at bottom)
   - Each seat row: seat number | side | athlete name
   - Coxswain row at bottom (if applicable)
   - Empty seats show "---" or "Empty"

4. Page header:
   - Lineup name (large, bold)
   - Date/time of export
   - Team name if available

5. No colors except black/white (ink-friendly)
6. Fixed width for consistent PDF capture (8.5" x 11" portrait)
7. Component renders off-screen (position: absolute, left: -9999px) until capture

Per LINE-11: "export lineup as print-ready PDF (high-contrast, large font)"

Reference existing printable patterns in codebase if any.
  </action>
  <verify>Component renders clean, readable lineup with large fonts and high contrast</verify>
  <done>PrintableLineup renders ink-friendly, high-contrast layout for PDF capture</done>
</task>

<task type="auto">
  <name>Task 2: Create lineupPdfExport utility</name>
  <files>src/v2/utils/lineupPdfExport.ts</files>
  <action>
Create PDF export utility using jsPDF + html2canvas:

1. Function signature:
   ```typescript
   export async function exportLineupToPdf(
     element: HTMLElement,
     lineupName: string,
     options?: { scale?: number; format?: 'a4' | 'letter' }
   ): Promise<void>
   ```

2. html2canvas configuration:
   - scale: 2 (high quality)
   - useCORS: true (for any images)
   - backgroundColor: '#ffffff'
   - logging: false (production)

3. jsPDF configuration:
   - orientation: 'portrait'
   - unit: 'mm'
   - format: 'letter' (US standard) or 'a4' (international)

4. Dimension calculation:
   - Per RESEARCH.md Pitfall 4: Calculate dimensions explicitly
   - Page width: pdf.internal.pageSize.getWidth()
   - Image width: page width - 20mm margins
   - Scale image height proportionally

5. Handle multi-page if content exceeds page height:
   - Check if imgHeight > pageHeight - margins
   - If so, either scale down or split across pages
   - Simple approach: scale to fit single page

6. Add metadata:
   - PDF title: lineup name
   - Creation date in footer

7. Trigger download with filename: `${lineupName}-${date}.pdf`

Per RESEARCH.md: "html2canvas captures at actual pixel dimensions, jsPDF expects specific units"
  </action>
  <verify>exportLineupToPdf generates downloadable PDF with correct dimensions</verify>
  <done>lineupPdfExport utility generates properly sized, downloadable PDF</done>
</task>

<task type="auto">
  <name>Task 3: Create ExportPDFButton and integrate into toolbar</name>
  <files>src/v2/components/lineup/ExportPDFButton.tsx, src/v2/components/lineup/LineupToolbar.tsx</files>
  <action>
Create export button and integrate PDF export flow:

1. ExportPDFButton component:
   - Props: lineupName, boats (current lineup data)
   - Icon: Lucide Download or FileDown
   - Loading state during PDF generation
   - Renders PrintableLineup off-screen (hidden div)
   - On click:
     a. Render PrintableLineup to hidden container
     b. Wait for render complete (useEffect or timeout)
     c. Call exportLineupToPdf with container element
     d. Clean up hidden container
   - Show error toast if generation fails

2. Use React ref to access PrintableLineup DOM:
   ```typescript
   const printRef = useRef<HTMLDivElement>(null);

   const handleExport = async () => {
     if (!printRef.current) return;
     setLoading(true);
     try {
       await exportLineupToPdf(printRef.current, lineupName);
     } catch (error) {
       toast.error('Failed to generate PDF');
     } finally {
       setLoading(false);
     }
   };
   ```

3. Integrate into LineupToolbar:
   - Add ExportPDFButton to toolbar
   - Layout: [Undo] [Redo] | [Save] [History ▼] | [Export PDF]
   - Disabled when no boats in workspace

4. Update index.ts exports
  </action>
  <verify>Clicking export generates and downloads PDF with lineup data</verify>
  <done>ExportPDFButton triggers PDF download with print-ready lineup (LINE-11)</done>
</task>

</tasks>

<verification>
1. Export button visible in toolbar
2. Clicking export generates PDF file download
3. PDF contains lineup name, date, all boats and assignments
4. PDF has large, readable fonts (14-24pt)
5. PDF is high-contrast (black on white)
6. PDF fits on single page (or handles multi-page gracefully)
7. Export works for lineups with 1-4 boats
8. Loading state shown during generation
</verification>

<success_criteria>
- LINE-11 complete: Coach can export lineup as print-ready PDF
- PDF readable at typical print distances (dock, boathouse)
- High contrast design suitable for printing
- Clean, professional appearance
</success_criteria>

<output>
After completion, create `.planning/phases/08-lineup-builder/08-06-SUMMARY.md`
</output>
