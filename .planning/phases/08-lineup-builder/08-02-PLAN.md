---
phase: 08-lineup-builder
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/v2/components/lineup/SeatSlot.tsx
  - src/v2/components/lineup/DraggableAthleteCard.tsx
  - src/v2/components/lineup/LineupWorkspace.tsx
  - src/v2/components/lineup/AthleteBank.tsx
  - src/v2/components/lineup/BoatView.tsx
autonomous: true

must_haves:
  truths:
    - "Coach can drag athlete from bank to empty seat"
    - "Coach can drag athlete from bank to occupied seat and athletes auto-swap"
    - "Coach can drag athlete between seats to rearrange"
    - "Coach can drag athlete back to bank to remove from seat"
    - "Full athlete card shows at cursor during drag"
  artifacts:
    - path: "src/v2/components/lineup/SeatSlot.tsx"
      provides: "Droppable seat with drop feedback and warning display"
      min_lines: 80
    - path: "src/v2/components/lineup/DraggableAthleteCard.tsx"
      provides: "Draggable athlete card with dnd-kit hooks"
      min_lines: 60
  key_links:
    - from: "src/v2/components/lineup/LineupWorkspace.tsx"
      to: "@dnd-kit/core"
      via: "DndContext with sensors"
      pattern: "DndContext.*sensors"
    - from: "src/v2/components/lineup/LineupWorkspace.tsx"
      to: "src/store/lineupStore.js"
      via: "assignToSeat, swapAthletes"
      pattern: "(assignToSeat|swapAthletes)"
---

<objective>
Implement full drag-and-drop functionality with auto-swap behavior for the lineup builder.

Purpose: Enable coaches to assign athletes to seats via drag-drop, with automatic swapping when dropping on occupied seats. This is the core interaction for building lineups (LINE-02, LINE-03, LINE-04).

Output: Working drag-drop with DragOverlay showing full athlete card, droppable seat slots, and auto-swap logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lineup-builder/08-CONTEXT.md
@.planning/phases/08-lineup-builder/08-RESEARCH.md
@.planning/phases/08-lineup-builder/08-01-SUMMARY.md
@src/store/lineupStore.js
@src/utils/boatConfig.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DraggableAthleteCard component</name>
  <files>src/v2/components/lineup/DraggableAthleteCard.tsx</files>
  <action>
Create draggable athlete card using dnd-kit:

1. Import `useDraggable` from '@dnd-kit/core'
2. Props: athlete object, source (bank | seat position)
3. Use `useDraggable({ id: athlete.id, data: { athlete, source } })`
4. Apply transform during drag: `transform: translate3d(${x}px, ${y}px, 0)`
5. Show opacity:0.5 during drag (original card fades)
6. Display: name, avatar, side preference badge (P/S/Both)
7. Per CONTEXT.md: "Full athlete card shown at cursor during drag (name, photo/avatar, side preference)"
8. Cursor changes to 'grab' (idle) and 'grabbing' (dragging)
9. Card styling matches V2 design tokens

Source tracking is CRITICAL for auto-swap - must know where athlete came from.
  </action>
  <verify>Card can be picked up and cursor changes, original card fades during drag</verify>
  <done>DraggableAthleteCard works with dnd-kit, passes source position in drag data</done>
</task>

<task type="auto">
  <name>Task 2: Create SeatSlot component with drop handling</name>
  <files>src/v2/components/lineup/SeatSlot.tsx</files>
  <action>
Create droppable seat slot using dnd-kit:

1. Import `useDroppable` from '@dnd-kit/core'
2. Props: boatId, seat object (seatNumber, side, athlete), isCoxswain flag
3. Use `useDroppable({ id: \`${boatId}-seat-${seatNumber}\`, data: { boatId, seat, isCoxswain } })`
4. Visual feedback on drag-over (isOver):
   - Per CONTEXT.md: "Seats show glow/highlight border on drag-over (green=valid, red=invalid)"
   - Green border when valid drop target
   - Red border when invalid (validation check placeholder for plan 08-03)
5. Empty state: dashed border, "Empty" text
6. Occupied state: show DraggableAthleteCard (athlete in seat is also draggable for rearranging)
7. Display seat number (left), side indicator (right)
8. Seat styling: rounded corners, proper padding, V2 tokens

The SeatSlot wraps DraggableAthleteCard when occupied - athletes in seats can be dragged to other seats.
  </action>
  <verify>Seats highlight on drag-over, empty seats have dashed border, occupied seats show athlete card</verify>
  <done>SeatSlot is droppable with visual feedback, can contain draggable athlete cards</done>
</task>

<task type="auto">
  <name>Task 3: Wire drag-drop with DragOverlay and auto-swap in LineupWorkspace</name>
  <files>src/v2/components/lineup/LineupWorkspace.tsx, src/v2/components/lineup/AthleteBank.tsx, src/v2/components/lineup/BoatView.tsx</files>
  <action>
Implement full drag-drop flow with auto-swap:

1. LineupWorkspace: Configure DndContext with sensors
   ```
   const sensors = useSensors(
     useSensor(MouseSensor, { activationConstraint: { distance: 10 } }),
     useSensor(TouchSensor, { activationConstraint: { delay: 250, tolerance: 5 } }),
     useSensor(KeyboardSensor)
   );
   ```

2. Track active drag state for DragOverlay:
   - `const [activeId, setActiveId] = useState(null)`
   - `onDragStart`: Set activeId
   - `onDragEnd`: Clear activeId and handle drop

3. Implement onDragEnd with AUTO-SWAP logic:
   ```
   const handleDragEnd = (event) => {
     const { active, over } = event;
     if (!over) return;

     const draggedAthlete = active.data.current.athlete;
     const sourcePosition = active.data.current.source; // {boatId, seatNumber} or 'bank'
     const targetPosition = over.data.current; // {boatId, seat, isCoxswain}

     // Find if target seat is occupied
     const occupiedAthlete = targetPosition.seat.athlete;

     if (occupiedAthlete) {
       // AUTO-SWAP: Exchange positions
       // If source was bank: athlete goes to bank, dragged goes to seat
       // If source was seat: athletes swap seats
       // Use lineupStore actions (assignToSeat, removeFromSeat)
     } else {
       // Simple assignment
       assignToSeat(boatId, seatNumber, draggedAthlete);
       // If source was seat, remove from source
     }
   };
   ```

4. DragOverlay: Show full athlete card at cursor position during drag
   Per CONTEXT.md: "Full athlete card shown at cursor during drag (name, photo/avatar, side preference)"

5. Update AthleteBank: Wrap athletes in DraggableAthleteCard with source='bank'

6. Update BoatView: Use SeatSlot for each seat position

7. Handle drop back to bank: If dropped outside seats (or on bank area), remove athlete from seat

Per CONTEXT.md: "Dropping on occupied seat triggers auto-swap - athletes exchange places automatically"
Per RESEARCH.md: Track source position BEFORE state change to properly swap
  </action>
  <verify>Drag from bank to seat works, drag between seats swaps athletes, drag to bank removes athlete</verify>
  <done>Full drag-drop with auto-swap working: bank-to-seat, seat-to-seat, seat-to-bank</done>
</task>

</tasks>

<verification>
1. Can drag athlete from bank to empty seat - athlete appears in seat
2. Can drag athlete from bank to occupied seat - athletes swap (occupant goes to bank)
3. Can drag athlete from seat to different seat - athletes swap positions
4. Can drag athlete from seat back to bank - seat becomes empty
5. DragOverlay shows full athlete card during drag
6. Seat highlights green on drag-over
7. Undo/redo still works after drag operations (middleware tracks activeBoats changes)
</verification>

<success_criteria>
- All drag-drop operations work as specified (LINE-02, LINE-03, LINE-04)
- Auto-swap behavior correctly exchanges positions
- DragOverlay shows full athlete card at cursor
- Visual feedback on drop targets
- State changes tracked by undo middleware
</success_criteria>

<output>
After completion, create `.planning/phases/08-lineup-builder/08-02-SUMMARY.md`
</output>
