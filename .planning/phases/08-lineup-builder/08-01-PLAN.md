---
phase: 08-lineup-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2/components/lineup/LineupWorkspace.tsx
  - src/v2/components/lineup/AthleteBank.tsx
  - src/v2/components/lineup/BoatView.tsx
  - src/v2/components/lineup/AddBoatButton.tsx
  - src/v2/components/lineup/index.ts
  - src/v2/types/lineup.ts
autonomous: true

must_haves:
  truths:
    - "Coach sees lineup builder workspace with boat view and athlete bank"
    - "Coach sees available athletes in left sidebar bank"
    - "Coach sees selected boat with numbered seats arranged vertically"
    - "Coach can create new lineup for boat class (8+, 4+, 2x, etc.)"
  artifacts:
    - path: "src/v2/components/lineup/LineupWorkspace.tsx"
      provides: "Main workspace container with DndContext and boat management"
      min_lines: 100
    - path: "src/v2/components/lineup/AthleteBank.tsx"
      provides: "Left sidebar showing available athletes"
      min_lines: 60
    - path: "src/v2/components/lineup/BoatView.tsx"
      provides: "Vertical boat seats display (bow at top)"
      min_lines: 100
    - path: "src/v2/components/lineup/AddBoatButton.tsx"
      provides: "Button with boat class dropdown to create new boats"
      min_lines: 50
    - path: "src/v2/types/lineup.ts"
      provides: "TypeScript types for lineup builder components"
      exports: ["LineupWorkspaceProps", "AthleteBankProps", "BoatViewProps", "SeatSlotData", "AddBoatButtonProps"]
  key_links:
    - from: "src/v2/components/lineup/LineupWorkspace.tsx"
      to: "src/store/lineupStore.js"
      via: "useLineupStore hook"
      pattern: "useLineupStore"
    - from: "src/v2/components/lineup/AthleteBank.tsx"
      to: "getAvailableAthletes"
      via: "store method call"
      pattern: "getAvailableAthletes"
    - from: "src/v2/components/lineup/AddBoatButton.tsx"
      to: "useLineupStore().addBoat"
      via: "store method call"
      pattern: "addBoat"
---

<objective>
Build the foundational components for the V2 Lineup Builder workspace with boat creation.

Purpose: Establish the base UI structure with DndContext setup, athlete bank sidebar, boat view with vertical seat layout, and boat creation controls. This enables drag-drop functionality in subsequent plans and allows coaches to create new lineups.

Output: LineupWorkspace, AthleteBank, BoatView, and AddBoatButton components in V2 with TypeScript types.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lineup-builder/08-CONTEXT.md
@.planning/phases/08-lineup-builder/08-RESEARCH.md
@src/store/lineupStore.js
@src/utils/boatConfig.js
@src/v2/components/athletes/AthleteCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript types for lineup builder</name>
  <files>src/v2/types/lineup.ts</files>
  <action>
Create TypeScript types for the lineup builder components:

1. `LineupWorkspaceProps` - Props for main workspace container
2. `AthleteBankProps` - Props for athlete bank sidebar
3. `BoatViewProps` - Props for boat display with seats
4. `SeatSlotData` - Data structure for droppable seat slots
5. `DragData` - Data passed during drag operations (athlete info, source position)
6. `DropData` - Data for drop targets (boat ID, seat number, is coxswain)
7. `SeatWarning` - Warning type for validation issues (type, message)
8. `AddBoatButtonProps` - Props for boat creation button/dropdown
9. `BoatConfig` - Boat configuration type (name, numSeats, hasCoxswain)

Use existing types from src/v2/types/athletes.ts as reference for Athlete type.
Import and re-export Athlete type from athletes.ts to maintain single source of truth.
  </action>
  <verify>TypeScript compiles without errors: `npx tsc --noEmit src/v2/types/lineup.ts`</verify>
  <done>lineup.ts exports all required types for lineup builder components including AddBoatButtonProps</done>
</task>

<task type="auto">
  <name>Task 2: Create AddBoatButton component (LINE-01)</name>
  <files>src/v2/components/lineup/AddBoatButton.tsx</files>
  <action>
Create the boat creation UI component that allows coaches to add new boats to the workspace:

1. "Add Boat" button that opens a dropdown/popover with boat class options
2. Boat class options from `useLineupStore().boatConfigs`:
   - Standard rowing boat classes (8+, 4+, 4-, 2x, 2-, 1x, etc.)
   - Display name and number of seats for each option
3. On selection: call `useLineupStore().addBoat(boatConfig)` to add boat to workspace
4. Optional shell selector (dropdown of `useLineupStore().shells`) shown after boat class selection
5. Close dropdown after selection
6. Use V2 design tokens (bg-surface, text-primary, border-border, etc.)
7. Position: Fixed top-right of workspace or as floating action button

Per CONTEXT.md: Lineup builder allows "boat class selection (8+, 4+, 2x, etc.)"

Implementation notes:
- Use Radix Popover or simple dropdown pattern
- Boat configs come from store (already populated from API in V1)
- Shell name is optional - can be added/changed later
  </action>
  <verify>Click "Add Boat" button, select boat class (8+), verify boat appears in workspace</verify>
  <done>Coach can click "Add Boat", select boat class (8+, 4+, 2x, etc.), and see new boat appear in workspace</done>
</task>

<task type="auto">
  <name>Task 3: Create AthleteBank component</name>
  <files>src/v2/components/lineup/AthleteBank.tsx</files>
  <action>
Create left sidebar component showing available (unassigned) athletes:

1. Get available athletes from `useLineupStore().getAvailableAthletes()`
2. Display athletes in a scrollable list with search filter
3. Each athlete shows: name, avatar (use AthleteAvatar pattern), side preference badge (P/S/Both)
4. Athletes are NOT draggable yet (drag setup in plan 08-02) - just display
5. Show total count of available athletes at top
6. Use V2 design tokens (bg-surface, text-primary, etc.)
7. Layout: fixed width sidebar (280px), full height, scrollable

Per CONTEXT.md: "Athlete bank positioned in left sidebar - classic builder pattern"

Reference existing AthleteCard.tsx and AthleteAvatar.tsx for styling patterns.
  </action>
  <verify>Component renders list of athletes with search filter, visually inspect in Storybook or dev</verify>
  <done>AthleteBank shows searchable list of unassigned athletes with avatar and side preference</done>
</task>

<task type="auto">
  <name>Task 4: Create BoatView component</name>
  <files>src/v2/components/lineup/BoatView.tsx</files>
  <action>
Create boat display component with vertical seat arrangement:

1. Props: boat object from lineupStore (id, name, seats[], coxswain, shellName)
2. Display boat header: boat class name, shell name if set
3. Seats arranged VERTICALLY: bow (seat 1) at TOP, stroke at BOTTOM
4. Per CONTEXT.md: "Boat seats arranged vertically (bow at top, stern at bottom) - mirrors how lineups are written"
5. Each seat shows: seat number, side indicator (Port/Starboard), assigned athlete name or empty state
6. Coxswain position shown separately (at stern/bottom for stern-coxed boats)
7. Empty seats show "Empty" placeholder with dashed border
8. Occupied seats show athlete name and avatar thumbnail
9. Seats are NOT droppable yet (drop setup in plan 08-02) - just display
10. Use V2 design tokens and existing component patterns

Layout: vertical flex column with seats stacked, seat numbers on left, side on right.
  </action>
  <verify>Component renders boat with seats in correct vertical order (bow at top), correct side labels</verify>
  <done>BoatView displays boat with vertical seat arrangement, athlete names in occupied seats</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npm run build` succeeds
2. AddBoatButton opens dropdown with boat class options (8+, 4+, 2x, etc.)
3. Selecting boat class creates new boat in workspace via useLineupStore().addBoat()
4. AthleteBank renders with search filter and athlete list
5. BoatView renders with correct vertical seat arrangement
6. Seats show correct Port/Starboard designation
7. Components use V2 design tokens consistently
</verification>

<success_criteria>
- All four components created and TypeScript types defined
- AddBoatButton allows coach to select boat class and create new boat (LINE-01)
- AthleteBank shows available athletes with search functionality
- BoatView displays seats vertically with bow at top
- Components follow V2 design patterns and token usage
- No drag-drop functionality yet (that's plan 08-02)
</success_criteria>

<output>
After completion, create `.planning/phases/08-lineup-builder/08-01-SUMMARY.md`
</output>
