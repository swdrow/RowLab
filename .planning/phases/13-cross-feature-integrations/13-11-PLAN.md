---
phase: 13-cross-feature-integrations
plan: 11
type: execute
wave: 4
depends_on: ["13-05", "13-06", "13-10"]
files_modified:
  - src/v2/pages/training/SessionsPage.tsx
  - src/v2/pages/training/SessionDetailPage.tsx
  - src/v2/pages/training/LiveSessionPage.tsx
  - src/v2/App.tsx
autonomous: true

must_haves:
  truths:
    - "Sessions page shows list of sessions"
    - "Session detail page shows pieces and can launch live mode"
    - "Live session page shows real-time erg data"
    - "Routes are registered in App.tsx"
  artifacts:
    - path: "src/v2/pages/training/SessionsPage.tsx"
      provides: "Sessions list page"
      min_lines: 50
    - path: "src/v2/pages/training/SessionDetailPage.tsx"
      provides: "Session detail view with live launch"
      min_lines: 80
    - path: "src/v2/pages/training/LiveSessionPage.tsx"
      provides: "Live erg monitoring page"
      min_lines: 50
  key_links:
    - from: "src/v2/pages/training/SessionDetailPage.tsx"
      to: "src/v2/pages/training/LiveSessionPage.tsx"
      via: "navigate"
      pattern: "live"
---

<objective>
Create Session pages with calendar integration and live erg launch.

Purpose: Provide the full session workflow: viewing sessions, seeing session details with pieces, and launching live erg monitoring from a scheduled session.

Output: SessionsPage, SessionDetailPage, LiveSessionPage, route registration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-feature-integrations/13-CONTEXT.md
@.planning/phases/13-cross-feature-integrations/13-05-SUMMARY.md
@.planning/phases/13-cross-feature-integrations/13-06-SUMMARY.md
@.planning/phases/13-cross-feature-integrations/13-10-SUMMARY.md
@src/v2/App.tsx (reference for route registration)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionsPage</name>
  <files>src/v2/pages/training/SessionsPage.tsx</files>
  <action>
Create directories and SessionsPage:
```bash
mkdir -p src/v2/pages/training
```

Create SessionsPage.tsx:
```typescript
import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { Plus, Calendar, ListBullets, CaretRight, Play } from '@phosphor-icons/react';
import { useSessions } from '../../hooks/useSessions';
import { SessionForm } from '../../features/sessions/components/SessionForm';
import { Breadcrumbs } from '../../features/shared/components/Breadcrumbs';
import type { SessionType, SessionStatus } from '../../types/session';

const SESSION_TYPE_COLORS: Record<SessionType, string> = {
  ERG: 'bg-blue-500/10 text-blue-500',
  ROW: 'bg-green-500/10 text-green-500',
  LIFT: 'bg-amber-500/10 text-amber-500',
  RUN: 'bg-orange-500/10 text-orange-500',
  CROSS_TRAIN: 'bg-violet-500/10 text-violet-500',
  RECOVERY: 'bg-pink-500/10 text-pink-500',
};

const SESSION_STATUS_COLORS: Record<SessionStatus, string> = {
  PLANNED: 'bg-surface-default text-txt-secondary',
  ACTIVE: 'bg-green-500/10 text-green-500',
  COMPLETED: 'bg-blue-500/10 text-blue-500',
  CANCELLED: 'bg-red-500/10 text-red-500',
};

export function SessionsPage() {
  const navigate = useNavigate();
  const [showForm, setShowForm] = useState(false);
  const [viewMode, setViewMode] = useState<'list' | 'calendar'>('list');

  const { data, isLoading, isError } = useSessions();

  if (isLoading) {
    return (
      <div className="p-6 space-y-4">
        <div className="h-8 bg-surface-default rounded w-48 animate-pulse" />
        <div className="space-y-3">
          {[...Array(5)].map((_, i) => (
            <div key={i} className="h-20 bg-surface-elevated rounded-lg animate-pulse" />
          ))}
        </div>
      </div>
    );
  }

  if (isError) {
    return (
      <div className="p-6">
        <div className="bg-red-500/10 border border-red-500/20 rounded-lg p-4 text-red-500">
          Failed to load sessions. Please try again.
        </div>
      </div>
    );
  }

  return (
    <div className="p-6 space-y-6">
      {/* Header */}
      <div className="flex items-center justify-between">
        <div>
          <Breadcrumbs
            items={[
              { label: 'Training', href: '/app/training' },
              { label: 'Sessions' },
            ]}
          />
          <h1 className="text-2xl font-semibold text-txt-primary mt-2">Sessions</h1>
        </div>

        <div className="flex items-center gap-2">
          {/* View toggle */}
          <div className="flex items-center border border-bdr-default rounded-lg overflow-hidden">
            <button
              onClick={() => setViewMode('list')}
              className={`p-2 ${viewMode === 'list' ? 'bg-accent-primary/10 text-accent-primary' : 'text-txt-secondary'}`}
              title="List view"
            >
              <ListBullets className="w-5 h-5" />
            </button>
            <button
              onClick={() => setViewMode('calendar')}
              className={`p-2 ${viewMode === 'calendar' ? 'bg-accent-primary/10 text-accent-primary' : 'text-txt-secondary'}`}
              title="Calendar view"
            >
              <Calendar className="w-5 h-5" />
            </button>
          </div>

          <button
            onClick={() => setShowForm(true)}
            className="flex items-center gap-2 px-4 py-2 rounded-lg bg-accent-primary text-white
              font-medium hover:bg-accent-primary/90 transition-colors"
          >
            <Plus className="w-5 h-5" />
            New Session
          </button>
        </div>
      </div>

      {/* Session List */}
      {viewMode === 'list' && (
        <div className="space-y-3">
          {data?.sessions.length === 0 ? (
            <div className="text-center py-12">
              <Calendar className="w-12 h-12 text-txt-muted mx-auto mb-3" />
              <p className="text-txt-muted">No sessions yet</p>
              <button
                onClick={() => setShowForm(true)}
                className="mt-4 px-4 py-2 rounded-lg border border-bdr-default text-txt-secondary
                  hover:text-txt-primary hover:border-bdr-focus transition-colors"
              >
                Create your first session
              </button>
            </div>
          ) : (
            data?.sessions.map((session) => (
              <Link
                key={session.id}
                to={`/app/training/sessions/${session.id}`}
                className="block bg-surface-elevated rounded-lg border border-bdr-default p-4
                  hover:border-bdr-focus transition-colors group"
              >
                <div className="flex items-center justify-between">
                  <div className="flex items-center gap-4">
                    {/* Type badge */}
                    <span className={`px-2 py-1 rounded text-xs font-medium ${SESSION_TYPE_COLORS[session.type]}`}>
                      {session.type}
                    </span>

                    {/* Session info */}
                    <div>
                      <div className="font-medium text-txt-primary group-hover:text-accent-primary transition-colors">
                        {session.name}
                      </div>
                      <div className="text-sm text-txt-secondary">
                        {new Date(session.date).toLocaleDateString()} · {session.pieces.length} pieces
                      </div>
                    </div>
                  </div>

                  <div className="flex items-center gap-3">
                    {/* Status */}
                    <span className={`px-2 py-1 rounded text-xs font-medium ${SESSION_STATUS_COLORS[session.status]}`}>
                      {session.status}
                    </span>

                    {/* Live button if active */}
                    {session.status === 'ACTIVE' && (
                      <button
                        onClick={(e) => {
                          e.preventDefault();
                          navigate(`/app/training/sessions/${session.id}/live`);
                        }}
                        className="flex items-center gap-1 px-3 py-1.5 rounded-lg bg-green-500 text-white
                          text-sm font-medium hover:bg-green-600 transition-colors"
                      >
                        <Play className="w-4 h-4" weight="fill" />
                        Live
                      </button>
                    )}

                    <CaretRight className="w-5 h-5 text-txt-muted group-hover:text-txt-secondary transition-colors" />
                  </div>
                </div>
              </Link>
            ))
          )}
        </div>
      )}

      {/* Calendar View (placeholder) */}
      {viewMode === 'calendar' && (
        <div className="bg-surface-elevated rounded-lg border border-bdr-default p-8 text-center text-txt-muted">
          Calendar view coming soon. Use the existing Training Calendar for now.
        </div>
      )}

      {/* Create Session Modal */}
      {showForm && (
        <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
          <div className="bg-surface-elevated rounded-xl border border-bdr-default max-w-2xl w-full max-h-[90vh] overflow-y-auto">
            <div className="p-6">
              <h2 className="text-xl font-semibold text-txt-primary mb-6">Create Session</h2>
              <SessionForm
                onSuccess={() => setShowForm(false)}
                onCancel={() => setShowForm(false)}
              />
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>npx tsc --noEmit src/v2/pages/training/SessionsPage.tsx</verify>
  <done>SessionsPage compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create SessionDetailPage</name>
  <files>src/v2/pages/training/SessionDetailPage.tsx</files>
  <action>
Create SessionDetailPage.tsx:
```typescript
import { useParams, useNavigate, Link } from 'react-router-dom';
import { Play, Pencil, Trash, Clock, Users, Copy, Calendar } from '@phosphor-icons/react';
import { useSession, useUpdateSession, useDeleteSession } from '../../hooks/useSessions';
import { Breadcrumbs } from '../../features/shared/components/Breadcrumbs';
import type { PieceSegment } from '../../types/session';

const SEGMENT_ORDER: PieceSegment[] = ['WARMUP', 'MAIN', 'COOLDOWN'];
const SEGMENT_LABELS: Record<PieceSegment, string> = {
  WARMUP: 'Warmup',
  MAIN: 'Main Set',
  COOLDOWN: 'Cooldown',
};

export function SessionDetailPage() {
  const { sessionId } = useParams<{ sessionId: string }>();
  const navigate = useNavigate();

  const { data: session, isLoading, isError } = useSession(sessionId || '');
  const updateSession = useUpdateSession();
  const deleteSession = useDeleteSession();

  const handleStartLive = async () => {
    if (!sessionId) return;

    try {
      await updateSession.mutateAsync({
        sessionId,
        input: { status: 'ACTIVE' },
      });
      navigate(`/app/training/sessions/${sessionId}/live`);
    } catch (error) {
      console.error('Failed to start session:', error);
    }
  };

  const handleDelete = async () => {
    if (!sessionId || !confirm('Are you sure you want to delete this session?')) return;

    try {
      await deleteSession.mutateAsync(sessionId);
      navigate('/app/training/sessions');
    } catch (error) {
      console.error('Failed to delete session:', error);
    }
  };

  const formatTime = (seconds: number) => {
    const mins = Math.floor(seconds / 60);
    const secs = Math.floor(seconds % 60);
    return `${mins}:${secs.toString().padStart(2, '0')}`;
  };

  if (isLoading) {
    return (
      <div className="p-6 space-y-6">
        <div className="h-8 bg-surface-default rounded w-48 animate-pulse" />
        <div className="h-24 bg-surface-elevated rounded-lg animate-pulse" />
        <div className="space-y-3">
          {[...Array(3)].map((_, i) => (
            <div key={i} className="h-16 bg-surface-elevated rounded-lg animate-pulse" />
          ))}
        </div>
      </div>
    );
  }

  if (isError || !session) {
    return (
      <div className="p-6">
        <div className="bg-red-500/10 border border-red-500/20 rounded-lg p-4 text-red-500">
          Session not found or failed to load.
        </div>
      </div>
    );
  }

  // Group pieces by segment
  const piecesBySegment = SEGMENT_ORDER.map((segment) => ({
    segment,
    label: SEGMENT_LABELS[segment],
    pieces: session.pieces.filter((p) => p.segment === segment),
  })).filter((g) => g.pieces.length > 0);

  return (
    <div className="p-6 space-y-6">
      {/* Header */}
      <div>
        <Breadcrumbs
          items={[
            { label: 'Training', href: '/app/training' },
            { label: 'Sessions', href: '/app/training/sessions' },
            { label: session.name },
          ]}
        />
      </div>

      {/* Session Info Card */}
      <div className="bg-surface-elevated rounded-lg border border-bdr-default p-6">
        <div className="flex items-start justify-between">
          <div>
            <div className="flex items-center gap-3 mb-2">
              <h1 className="text-2xl font-semibold text-txt-primary">{session.name}</h1>
              <span className="px-2 py-1 rounded text-xs font-medium bg-blue-500/10 text-blue-500">
                {session.type}
              </span>
              <span className={`px-2 py-1 rounded text-xs font-medium ${
                session.status === 'ACTIVE'
                  ? 'bg-green-500/10 text-green-500'
                  : 'bg-surface-default text-txt-secondary'
              }`}>
                {session.status}
              </span>
            </div>

            <div className="flex items-center gap-4 text-txt-secondary">
              <span className="flex items-center gap-1">
                <Calendar className="w-4 h-4" />
                {new Date(session.date).toLocaleDateString()}
              </span>
              {session.startTime && (
                <span className="flex items-center gap-1">
                  <Clock className="w-4 h-4" />
                  {new Date(session.startTime).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}
                </span>
              )}
              <span className="flex items-center gap-1">
                <Users className="w-4 h-4" />
                {session.pieces.length} pieces
              </span>
            </div>

            {session.notes && (
              <p className="mt-3 text-txt-secondary">{session.notes}</p>
            )}

            {session.sessionCode && (
              <div className="mt-3 flex items-center gap-2">
                <span className="text-txt-muted">Session Code:</span>
                <span className="font-mono font-bold text-accent-primary">{session.sessionCode}</span>
                <button
                  onClick={() => navigator.clipboard.writeText(session.sessionCode || '')}
                  className="p-1 rounded hover:bg-surface-hover transition-colors"
                  title="Copy code"
                >
                  <Copy className="w-4 h-4 text-txt-muted" />
                </button>
              </div>
            )}
          </div>

          <div className="flex items-center gap-2">
            {session.status === 'PLANNED' && (
              <button
                onClick={handleStartLive}
                disabled={updateSession.isPending}
                className="flex items-center gap-2 px-4 py-2 rounded-lg bg-green-500 text-white
                  font-medium hover:bg-green-600 disabled:opacity-50 transition-colors"
              >
                <Play className="w-5 h-5" weight="fill" />
                Start Live
              </button>
            )}

            {session.status === 'ACTIVE' && (
              <Link
                to={`/app/training/sessions/${session.id}/live`}
                className="flex items-center gap-2 px-4 py-2 rounded-lg bg-green-500 text-white
                  font-medium hover:bg-green-600 transition-colors"
              >
                <Play className="w-5 h-5" weight="fill" />
                View Live
              </Link>
            )}

            <button
              className="p-2 rounded-lg border border-bdr-default text-txt-secondary
                hover:text-txt-primary hover:border-bdr-focus transition-colors"
              title="Edit session"
            >
              <Pencil className="w-5 h-5" />
            </button>

            <button
              onClick={handleDelete}
              className="p-2 rounded-lg border border-bdr-default text-txt-secondary
                hover:text-red-500 hover:border-red-500/50 transition-colors"
              title="Delete session"
            >
              <Trash className="w-5 h-5" />
            </button>
          </div>
        </div>
      </div>

      {/* Pieces */}
      <div className="space-y-4">
        <h2 className="text-lg font-medium text-txt-primary">Session Pieces</h2>

        {piecesBySegment.length === 0 ? (
          <div className="bg-surface-elevated rounded-lg border border-bdr-default p-8 text-center text-txt-muted">
            No pieces defined for this session.
          </div>
        ) : (
          piecesBySegment.map((group) => (
            <div key={group.segment} className="space-y-2">
              <h3 className="text-sm font-medium text-txt-muted uppercase tracking-wide">
                {group.label}
              </h3>
              <div className="space-y-2">
                {group.pieces.map((piece, index) => (
                  <div
                    key={piece.id}
                    className="bg-surface-elevated rounded-lg border border-bdr-default p-4"
                  >
                    <div className="flex items-center justify-between">
                      <div>
                        <div className="font-medium text-txt-primary">{piece.name}</div>
                        {piece.description && (
                          <div className="text-sm text-txt-secondary mt-1">{piece.description}</div>
                        )}
                      </div>

                      <div className="flex items-center gap-4 text-sm text-txt-secondary">
                        {piece.distance && <span>{piece.distance}m</span>}
                        {piece.duration && <span>{formatTime(piece.duration)}</span>}
                        {piece.targetSplit && (
                          <span className="font-mono">@{formatTime(piece.targetSplit)}/500m</span>
                        )}
                        {piece.targetRate && <span>{piece.targetRate}spm</span>}
                      </div>
                    </div>

                    {piece.notes && (
                      <div className="mt-2 text-sm text-txt-muted">{piece.notes}</div>
                    )}
                  </div>
                ))}
              </div>
            </div>
          ))
        )}
      </div>
    </div>
  );
}
```
  </action>
  <verify>npx tsc --noEmit src/v2/pages/training/SessionDetailPage.tsx</verify>
  <done>SessionDetailPage compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create LiveSessionPage and register routes</name>
  <files>src/v2/pages/training/LiveSessionPage.tsx, src/v2/App.tsx</files>
  <action>
Create LiveSessionPage.tsx:
```typescript
import { useParams, Link } from 'react-router-dom';
import { ArrowLeft, Stop } from '@phosphor-icons/react';
import { useSession, useUpdateSession } from '../../hooks/useSessions';
import { LiveErgDashboard } from '../../features/live-erg/components/LiveErgDashboard';
import { Breadcrumbs } from '../../features/shared/components/Breadcrumbs';

export function LiveSessionPage() {
  const { sessionId } = useParams<{ sessionId: string }>();
  const { data: session, isLoading, isError } = useSession(sessionId || '');
  const updateSession = useUpdateSession();

  const handleEndSession = async () => {
    if (!sessionId || !confirm('Are you sure you want to end this session?')) return;

    try {
      await updateSession.mutateAsync({
        sessionId,
        input: { status: 'COMPLETED' },
      });
    } catch (error) {
      console.error('Failed to end session:', error);
    }
  };

  if (isLoading) {
    return (
      <div className="p-6">
        <div className="animate-spin w-8 h-8 border-2 border-accent-primary border-t-transparent rounded-full mx-auto" />
      </div>
    );
  }

  if (isError || !session) {
    return (
      <div className="p-6">
        <div className="bg-red-500/10 border border-red-500/20 rounded-lg p-4 text-red-500">
          Session not found or failed to load.
        </div>
      </div>
    );
  }

  if (session.status !== 'ACTIVE') {
    return (
      <div className="p-6">
        <Breadcrumbs
          items={[
            { label: 'Training', href: '/app/training' },
            { label: 'Sessions', href: '/app/training/sessions' },
            { label: session.name, href: `/app/training/sessions/${session.id}` },
            { label: 'Live' },
          ]}
        />
        <div className="mt-6 bg-amber-500/10 border border-amber-500/20 rounded-lg p-4 text-amber-500">
          <p>This session is not currently active.</p>
          <Link
            to={`/app/training/sessions/${session.id}`}
            className="inline-flex items-center gap-2 mt-2 text-accent-primary hover:underline"
          >
            <ArrowLeft className="w-4 h-4" />
            Back to session details
          </Link>
        </div>
      </div>
    );
  }

  // Get target pace from first main piece (if erg session)
  const mainPiece = session.pieces.find((p) => p.segment === 'MAIN');
  const targetPace = mainPiece?.targetSplit;

  return (
    <div className="p-6 space-y-6">
      {/* Header */}
      <div className="flex items-center justify-between">
        <div>
          <Breadcrumbs
            items={[
              { label: 'Training', href: '/app/training' },
              { label: 'Sessions', href: '/app/training/sessions' },
              { label: session.name, href: `/app/training/sessions/${session.id}` },
              { label: 'Live' },
            ]}
          />
        </div>

        <button
          onClick={handleEndSession}
          disabled={updateSession.isPending}
          className="flex items-center gap-2 px-4 py-2 rounded-lg bg-red-500 text-white
            font-medium hover:bg-red-600 disabled:opacity-50 transition-colors"
        >
          <Stop className="w-5 h-5" weight="fill" />
          End Session
        </button>
      </div>

      {/* Live Dashboard */}
      <LiveErgDashboard
        sessionId={session.id}
        sessionName={session.name}
        sessionCode={session.sessionCode}
        targetPace={targetPace}
      />
    </div>
  );
}
```

Now update src/v2/App.tsx to register the routes. Find the routes section and add:

```typescript
// Add imports at top
import { SessionsPage } from './pages/training/SessionsPage';
import { SessionDetailPage } from './pages/training/SessionDetailPage';
import { LiveSessionPage } from './pages/training/LiveSessionPage';

// Add routes (inside the existing Routes structure, under /app/training)
<Route path="training/sessions" element={<SessionsPage />} />
<Route path="training/sessions/:sessionId" element={<SessionDetailPage />} />
<Route path="training/sessions/:sessionId/live" element={<LiveSessionPage />} />
```

Note: Preserve all existing routes. Just add these new session routes.
  </action>
  <verify>npx tsc --noEmit src/v2/pages/training/LiveSessionPage.tsx</verify>
  <done>All pages compile and routes registered</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit src/v2/pages/training/SessionsPage.tsx` passes
2. `npx tsc --noEmit src/v2/pages/training/SessionDetailPage.tsx` passes
3. `npx tsc --noEmit src/v2/pages/training/LiveSessionPage.tsx` passes
4. `npx tsc --noEmit` passes (full project)
5. Visual test: Navigate /app/training/sessions, create session, view details, start live
</verification>

<success_criteria>
- SessionsPage shows list of sessions with type and status badges
- Can create new session from modal
- SessionDetailPage shows session with pieces grouped by segment
- "Start Live" button changes status to ACTIVE and navigates to live page
- LiveSessionPage shows real-time erg data with polling
- "End Session" changes status to COMPLETED
- Session code displayed for athlete joining
- All routes work: /app/training/sessions, /:id, /:id/live
</success_criteria>

<output>
After completion, create `.planning/phases/13-cross-feature-integrations/13-11-SUMMARY.md`
</output>
