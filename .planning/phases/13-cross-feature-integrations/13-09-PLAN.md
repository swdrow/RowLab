---
phase: 13-cross-feature-integrations
plan: 09
type: execute
wave: 3
depends_on: ["13-04"]
files_modified:
  - src/v2/features/shared/components/HoverCard.tsx
  - src/v2/features/shared/components/Breadcrumbs.tsx
  - src/v2/features/shared/components/QuickNav.tsx
  - src/v2/components/shell/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Hover cards show preview on hover"
    - "Breadcrumbs show navigation path"
    - "Search icon in header opens command palette"
    - "Quick links provide cross-feature navigation"
  artifacts:
    - path: "src/v2/features/shared/components/HoverCard.tsx"
      provides: "Radix-based hover card for entity preview"
      min_lines: 50
    - path: "src/v2/features/shared/components/Breadcrumbs.tsx"
      provides: "Navigation breadcrumbs component"
      min_lines: 30
  key_links:
    - from: "src/v2/components/shell/Header.tsx"
      to: "src/v2/features/search/components/CommandPalette.tsx"
      via: "component render"
      pattern: "CommandPalette"
---

<objective>
Create cross-feature navigation components.

Purpose: Enable seamless navigation between related entities with hover cards for preview, breadcrumbs for orientation, and global search integration in the header.

Output: HoverCard, Breadcrumbs, QuickNav components, Header integration with search.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-feature-integrations/13-CONTEXT.md
@.planning/phases/13-cross-feature-integrations/13-RESEARCH.md
@.planning/phases/13-cross-feature-integrations/13-04-SUMMARY.md
@src/v2/components/shell/Header.tsx (reference for header structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HoverCard component</name>
  <files>src/v2/features/shared/components/HoverCard.tsx</files>
  <action>
Create directories and component:
```bash
mkdir -p src/v2/features/shared/components
```

Create HoverCard.tsx using Radix UI:
```typescript
import * as RadixHoverCard from '@radix-ui/react-hover-card';
import { motion, AnimatePresence } from 'framer-motion';
import { ReactNode, useState } from 'react';

interface HoverCardProps {
  children: ReactNode;
  content: ReactNode;
  side?: 'top' | 'right' | 'bottom' | 'left';
  align?: 'start' | 'center' | 'end';
  openDelay?: number;
  closeDelay?: number;
}

export function HoverCard({
  children,
  content,
  side = 'bottom',
  align = 'center',
  openDelay = 300,
  closeDelay = 100,
}: HoverCardProps) {
  const [open, setOpen] = useState(false);

  return (
    <RadixHoverCard.Root
      open={open}
      onOpenChange={setOpen}
      openDelay={openDelay}
      closeDelay={closeDelay}
    >
      <RadixHoverCard.Trigger asChild>{children}</RadixHoverCard.Trigger>

      <AnimatePresence>
        {open && (
          <RadixHoverCard.Portal forceMount>
            <RadixHoverCard.Content
              asChild
              side={side}
              align={align}
              sideOffset={8}
            >
              <motion.div
                initial={{ opacity: 0, scale: 0.95 }}
                animate={{ opacity: 1, scale: 1 }}
                exit={{ opacity: 0, scale: 0.95 }}
                transition={{ duration: 0.15 }}
                className="bg-surface-elevated rounded-lg border border-bdr-default shadow-xl p-4 z-50 max-w-xs"
              >
                {content}
                <RadixHoverCard.Arrow className="fill-surface-elevated" />
              </motion.div>
            </RadixHoverCard.Content>
          </RadixHoverCard.Portal>
        )}
      </AnimatePresence>
    </RadixHoverCard.Root>
  );
}

// Pre-built hover cards for common entities
interface AthleteHoverCardProps {
  athlete: {
    id: string;
    firstName: string;
    lastName: string;
    sidePreference?: string;
    avatar?: string;
    latestErgTest?: {
      testType: string;
      time: number;
    };
  };
  children: ReactNode;
}

export function AthleteHoverCard({ athlete, children }: AthleteHoverCardProps) {
  const formatErgTime = (seconds: number) => {
    const mins = Math.floor(seconds / 60);
    const secs = Math.floor(seconds % 60);
    return `${mins}:${secs.toString().padStart(2, '0')}`;
  };

  return (
    <HoverCard
      content={
        <div className="space-y-2">
          <div className="flex items-center gap-3">
            {athlete.avatar ? (
              <img
                src={athlete.avatar}
                alt={`${athlete.firstName} ${athlete.lastName}`}
                className="w-10 h-10 rounded-full"
              />
            ) : (
              <div className="w-10 h-10 rounded-full bg-accent-primary/20 flex items-center justify-center text-accent-primary font-medium">
                {athlete.firstName[0]}{athlete.lastName[0]}
              </div>
            )}
            <div>
              <div className="font-medium text-txt-primary">
                {athlete.firstName} {athlete.lastName}
              </div>
              {athlete.sidePreference && (
                <div className="text-sm text-txt-secondary">
                  {athlete.sidePreference} side
                </div>
              )}
            </div>
          </div>

          {athlete.latestErgTest && (
            <div className="pt-2 border-t border-bdr-default">
              <div className="text-xs text-txt-muted">Latest {athlete.latestErgTest.testType}</div>
              <div className="font-mono text-txt-primary">
                {formatErgTime(athlete.latestErgTest.time)}
              </div>
            </div>
          )}
        </div>
      }
    >
      {children}
    </HoverCard>
  );
}

interface SessionHoverCardProps {
  session: {
    id: string;
    name: string;
    type: string;
    date: string;
    pieces?: Array<{ name: string }>;
  };
  children: ReactNode;
}

export function SessionHoverCard({ session, children }: SessionHoverCardProps) {
  return (
    <HoverCard
      content={
        <div className="space-y-2">
          <div>
            <div className="font-medium text-txt-primary">{session.name}</div>
            <div className="text-sm text-txt-secondary">
              {session.type} · {new Date(session.date).toLocaleDateString()}
            </div>
          </div>

          {session.pieces && session.pieces.length > 0 && (
            <div className="pt-2 border-t border-bdr-default">
              <div className="text-xs text-txt-muted mb-1">Pieces</div>
              <div className="space-y-1">
                {session.pieces.slice(0, 3).map((piece, i) => (
                  <div key={i} className="text-sm text-txt-secondary">{piece.name}</div>
                ))}
                {session.pieces.length > 3 && (
                  <div className="text-sm text-txt-muted">+{session.pieces.length - 3} more</div>
                )}
              </div>
            </div>
          )}
        </div>
      }
    >
      {children}
    </HoverCard>
  );
}
```
  </action>
  <verify>npx tsc --noEmit src/v2/features/shared/components/HoverCard.tsx</verify>
  <done>HoverCard component compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Breadcrumbs component</name>
  <files>src/v2/features/shared/components/Breadcrumbs.tsx</files>
  <action>
Create Breadcrumbs.tsx:
```typescript
import { Link, useLocation } from 'react-router-dom';
import { CaretRight, House } from '@phosphor-icons/react';

interface BreadcrumbItem {
  label: string;
  href?: string;
}

interface BreadcrumbsProps {
  items?: BreadcrumbItem[];
  showHome?: boolean;
}

// Generate breadcrumbs from route path
function generateBreadcrumbs(pathname: string): BreadcrumbItem[] {
  const segments = pathname.split('/').filter(Boolean);
  const items: BreadcrumbItem[] = [];

  // Map route segments to labels
  const labelMap: Record<string, string> = {
    app: 'Home',
    athletes: 'Athletes',
    erg: 'Erg Tests',
    lineups: 'Lineups',
    'seat-racing': 'Seat Racing',
    training: 'Training',
    regattas: 'Regattas',
    settings: 'Settings',
    sessions: 'Sessions',
  };

  let currentPath = '';
  segments.forEach((segment, index) => {
    currentPath += `/${segment}`;

    // Skip 'app' in breadcrumb display but include in path
    if (segment === 'app' && index === 0) return;

    // Check if this is an ID (cuid pattern) - show as detail
    const isId = segment.length > 15 || /^[a-z0-9]+$/.test(segment);

    if (isId) {
      items.push({
        label: 'Details',
        href: index < segments.length - 1 ? currentPath : undefined,
      });
    } else {
      items.push({
        label: labelMap[segment] || segment.charAt(0).toUpperCase() + segment.slice(1),
        href: index < segments.length - 1 ? currentPath : undefined,
      });
    }
  });

  return items;
}

export function Breadcrumbs({ items, showHome = true }: BreadcrumbsProps) {
  const location = useLocation();

  // Use provided items or generate from location
  const breadcrumbs = items || generateBreadcrumbs(location.pathname);

  if (breadcrumbs.length === 0) return null;

  return (
    <nav aria-label="Breadcrumb" className="flex items-center gap-1 text-sm">
      {showHome && (
        <>
          <Link
            to="/app"
            className="text-txt-muted hover:text-txt-primary transition-colors p-1 rounded hover:bg-surface-hover"
            aria-label="Home"
          >
            <House className="w-4 h-4" />
          </Link>
          {breadcrumbs.length > 0 && (
            <CaretRight className="w-4 h-4 text-txt-muted" />
          )}
        </>
      )}

      {breadcrumbs.map((item, index) => (
        <div key={index} className="flex items-center gap-1">
          {item.href ? (
            <Link
              to={item.href}
              className="text-txt-muted hover:text-txt-primary transition-colors"
            >
              {item.label}
            </Link>
          ) : (
            <span className="text-txt-primary font-medium">{item.label}</span>
          )}

          {index < breadcrumbs.length - 1 && (
            <CaretRight className="w-4 h-4 text-txt-muted" />
          )}
        </div>
      ))}
    </nav>
  );
}
```
  </action>
  <verify>npx tsc --noEmit src/v2/features/shared/components/Breadcrumbs.tsx</verify>
  <done>Breadcrumbs component compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Integrate search into Header</name>
  <files>src/v2/components/shell/Header.tsx</files>
  <action>
Update the Header component to include the global search button and command palette. Read the existing Header.tsx first, then add:

1. Import the CommandPalette and useCommandPalette:
```typescript
import { CommandPalette, useCommandPalette } from '../../features/search/components/CommandPalette';
import { MagnifyingGlass } from '@phosphor-icons/react';
```

2. Add the hook inside the component:
```typescript
const { open: searchOpen, setOpen: setSearchOpen } = useCommandPalette();
```

3. Add search button in the header (typically in the right section):
```typescript
<button
  onClick={() => setSearchOpen(true)}
  className="flex items-center gap-2 px-3 py-1.5 rounded-lg bg-surface-default border border-bdr-default
    text-txt-secondary hover:text-txt-primary hover:border-bdr-focus transition-colors"
  aria-label="Search"
>
  <MagnifyingGlass className="w-4 h-4" />
  <span className="text-sm hidden sm:inline">Search</span>
  <kbd className="hidden sm:inline px-1.5 py-0.5 text-xs rounded bg-surface-elevated border border-bdr-default ml-2">
    ⌘K
  </kbd>
</button>
```

4. Render the CommandPalette at the end of the component:
```typescript
<CommandPalette open={searchOpen} onOpenChange={setSearchOpen} />
```

Preserve all existing Header functionality while adding the search integration.
  </action>
  <verify>npx tsc --noEmit src/v2/components/shell/Header.tsx</verify>
  <done>Header includes search button and command palette</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit src/v2/features/shared/components/HoverCard.tsx` passes
2. `npx tsc --noEmit src/v2/features/shared/components/Breadcrumbs.tsx` passes
3. `npx tsc --noEmit src/v2/components/shell/Header.tsx` passes
4. Visual test: Search button visible in header, Cmd+K opens palette
</verification>

<success_criteria>
- HoverCard shows preview on hover with configurable delay
- AthleteHoverCard and SessionHoverCard provide entity-specific previews
- Breadcrumbs generate from route path automatically
- Search button in header opens command palette
- Cmd/Ctrl+K keyboard shortcut works globally
- All components integrate with existing V2 design system
</success_criteria>

<output>
After completion, create `.planning/phases/13-cross-feature-integrations/13-09-SUMMARY.md`
</output>
