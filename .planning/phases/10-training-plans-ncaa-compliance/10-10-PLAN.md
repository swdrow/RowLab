---
phase: 10-training-plans-ncaa-compliance
plan: 10
type: execute
wave: 6
depends_on: ["10-03", "10-05", "10-06", "10-07", "10-08", "10-09"]
files_modified:
  - src/v2/pages/CoachTrainingPage.tsx
  - src/v2/components/training/index.ts
  - src/app/routes/index.tsx
autonomous: false

must_haves:
  truths:
    - "Coach can access Training page from navigation"
    - "Training page shows calendar, compliance, and plan management tabs"
    - "All training components render without errors"
    - "Navigation and routing work correctly"
  artifacts:
    - path: "src/v2/pages/CoachTrainingPage.tsx"
      provides: "Main training page with tab navigation"
      min_lines: 150
    - path: "src/v2/components/training/index.ts"
      provides: "Barrel export for all training components"
  key_links:
    - from: "src/v2/pages/CoachTrainingPage.tsx"
      to: "src/v2/components/training/"
      via: "component imports"
      pattern: "from.*components/training"
    - from: "src/app/routes/index.tsx"
      to: "src/v2/pages/CoachTrainingPage.tsx"
      via: "route definition"
      pattern: "CoachTrainingPage"
---

<objective>
Integrate all training components into a cohesive page with navigation and routing.

Purpose: Complete the Training Plans & NCAA Compliance feature by assembling all components.
Output: CoachTrainingPage with tabs, routing integration, and human verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/v2/pages/CoachSeatRacingPage.tsx (pattern reference)
@src/v2/components/shell/WorkspaceSidebar.tsx (navigation reference)
@src/app/routes/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create training component barrel export</name>
  <files>src/v2/components/training/index.ts</files>
  <action>
Create index file exporting all training components:

```typescript
// src/v2/components/training/index.ts

// Calendar components
export {
  TrainingCalendar,
  DragDropCalendar,
  CalendarToolbar,
  WorkoutEventCard,
  getEventStyle,
} from './calendar';

// Workout components
export {
  WorkoutForm,
  ExerciseFieldArray,
} from './workouts';

// Periodization components
export {
  PeriodizationTimeline,
  BlockForm,
  TemplateApplicator,
} from './periodization';

// Assignment components
export {
  AssignmentManager,
  AthleteWorkoutView,
} from './assignments';

// Compliance components
export {
  ComplianceDashboard,
  WeeklyHoursTable,
  TrainingLoadChart,
  NCAA20HourWarning,
  NCAAWarningBadge,
  NCAAAuditReport,
} from './compliance';
```
  </action>
  <verify>
File exists and exports all training components.
  </verify>
  <done>
Barrel export provides single import point for all training components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CoachTrainingPage</name>
  <files>src/v2/pages/CoachTrainingPage.tsx</files>
  <action>
Create the main training page with tab navigation:

```typescript
// src/v2/pages/CoachTrainingPage.tsx

import React, { useState, useCallback } from 'react';
import { Tab } from '@headlessui/react';
import { format, startOfWeek } from 'date-fns';
import {
  DragDropCalendar,
  PeriodizationTimeline,
  ComplianceDashboard,
  WorkoutForm,
  AssignmentManager,
  NCAAAuditReport,
} from '../components/training';
import { useTrainingPlans } from '../hooks/useTrainingPlans';
import { useCreateWorkout } from '../hooks/useWorkouts';
import type { CalendarEvent, PeriodizationBlock } from '../types/training';
import { Dialog } from '@headlessui/react';

type TabType = 'calendar' | 'compliance' | 'plans';

/**
 * Coach training page with calendar, compliance, and plan management.
 */
export function CoachTrainingPage() {
  const [activeTab, setActiveTab] = useState<TabType>('calendar');
  const [selectedPlanId, setSelectedPlanId] = useState<string | null>(null);
  const [showWorkoutModal, setShowWorkoutModal] = useState(false);
  const [showAssignmentModal, setShowAssignmentModal] = useState(false);
  const [showAuditModal, setShowAuditModal] = useState(false);
  const [selectedSlot, setSelectedSlot] = useState<{ start: Date; end: Date } | null>(null);
  const [selectedEvent, setSelectedEvent] = useState<CalendarEvent | null>(null);

  const { plans, isLoading: loadingPlans } = useTrainingPlans();
  const { createWorkout, isCreating } = useCreateWorkout();

  // Demo periodization blocks (would come from API in production)
  const periodizationBlocks: PeriodizationBlock[] = [
    {
      id: '1',
      name: 'Spring Base',
      phase: 'base',
      startDate: '2026-01-06',
      endDate: '2026-03-01',
      weeklyTSSTarget: 400,
      focusAreas: ['Aerobic Endurance', 'Technique'],
      color: '#3b82f6',
    },
    {
      id: '2',
      name: 'Build Phase',
      phase: 'build',
      startDate: '2026-03-02',
      endDate: '2026-04-15',
      weeklyTSSTarget: 500,
      focusAreas: ['Power Development', 'Race Pace'],
      color: '#f59e0b',
    },
  ];

  const handleSelectSlot = useCallback((slotInfo: { start: Date; end: Date }) => {
    setSelectedSlot(slotInfo);
    setShowWorkoutModal(true);
  }, []);

  const handleSelectEvent = useCallback((event: CalendarEvent) => {
    setSelectedEvent(event);
    // Could open edit modal or slide-out panel
    console.log('Selected event:', event);
  }, []);

  const handleBlockClick = useCallback((block: PeriodizationBlock) => {
    console.log('Selected block:', block);
    // Could open edit modal
  }, []);

  const handleWorkoutSubmit = (data: any) => {
    createWorkout(data, {
      onSuccess: () => {
        setShowWorkoutModal(false);
        setSelectedSlot(null);
      },
    });
  };

  const handleAthleteClick = useCallback((athleteId: string) => {
    console.log('View athlete:', athleteId);
    // Could navigate to athlete training view
  }, []);

  const tabs = [
    { key: 'calendar', label: 'Calendar', icon: CalendarIcon },
    { key: 'compliance', label: 'Compliance', icon: ShieldIcon },
    { key: 'plans', label: 'Plans', icon: ClipboardIcon },
  ];

  return (
    <div className="coach-training-page">
      {/* Page Header */}
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-txt-primary">Training</h1>
        <p className="text-txt-secondary mt-1">
          Manage training plans, schedules, and NCAA compliance
        </p>
      </div>

      {/* Periodization Timeline */}
      <div className="mb-6 p-4 bg-surface-elevated rounded-lg border border-bdr-default">
        <PeriodizationTimeline
          blocks={periodizationBlocks}
          startDate={new Date('2026-01-01')}
          endDate={new Date('2026-06-30')}
          onBlockClick={handleBlockClick}
          onAddBlock={() => console.log('Add block')}
        />
      </div>

      {/* Tab Navigation */}
      <Tab.Group
        selectedIndex={tabs.findIndex((t) => t.key === activeTab)}
        onChange={(index) => setActiveTab(tabs[index].key as TabType)}
      >
        <Tab.List className="flex items-center gap-1 p-1 bg-surface-elevated rounded-lg mb-6">
          {tabs.map((tab) => (
            <Tab
              key={tab.key}
              className={({ selected }) =>
                `flex items-center gap-2 px-4 py-2 text-sm font-medium rounded-md transition-colors
                ${selected
                  ? 'bg-accent-primary text-white'
                  : 'text-txt-secondary hover:text-txt-primary hover:bg-surface-default'
                }`
              }
            >
              <tab.icon className="w-4 h-4" />
              {tab.label}
            </Tab>
          ))}
        </Tab.List>

        <Tab.Panels>
          {/* Calendar Tab */}
          <Tab.Panel>
            <div className="flex items-center justify-between mb-4">
              <div className="flex items-center gap-4">
                <select
                  value={selectedPlanId || ''}
                  onChange={(e) => setSelectedPlanId(e.target.value || null)}
                  className="px-3 py-2 bg-surface-default border border-bdr-default rounded-md
                             text-txt-primary text-sm
                             focus:outline-none focus:ring-2 focus:ring-accent-primary"
                >
                  <option value="">All Plans</option>
                  {plans.map((plan) => (
                    <option key={plan.id} value={plan.id}>{plan.name}</option>
                  ))}
                </select>
              </div>

              <button
                onClick={() => setShowWorkoutModal(true)}
                className="flex items-center gap-2 px-4 py-2 text-sm font-medium
                           bg-accent-primary text-white rounded-md
                           hover:bg-accent-primary-hover transition-colors"
              >
                <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
                </svg>
                Add Workout
              </button>
            </div>

            <DragDropCalendar
              planId={selectedPlanId || undefined}
              onSelectEvent={handleSelectEvent}
              onSelectSlot={handleSelectSlot}
              className="bg-surface-elevated rounded-lg border border-bdr-default p-4"
            />
          </Tab.Panel>

          {/* Compliance Tab */}
          <Tab.Panel>
            <div className="flex items-center justify-end mb-4">
              <button
                onClick={() => setShowAuditModal(true)}
                className="flex items-center gap-2 px-4 py-2 text-sm font-medium
                           border border-bdr-default text-txt-primary rounded-md
                           hover:bg-surface-elevated transition-colors"
              >
                <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
                Generate Audit Report
              </button>
            </div>

            <ComplianceDashboard onAthleteClick={handleAthleteClick} />
          </Tab.Panel>

          {/* Plans Tab */}
          <Tab.Panel>
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
              {/* Plan List */}
              <div className="lg:col-span-2">
                <div className="flex items-center justify-between mb-4">
                  <h3 className="text-lg font-semibold text-txt-primary">Training Plans</h3>
                  <button
                    className="flex items-center gap-2 px-4 py-2 text-sm font-medium
                               bg-accent-primary text-white rounded-md
                               hover:bg-accent-primary-hover transition-colors"
                  >
                    <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
                    </svg>
                    New Plan
                  </button>
                </div>

                {loadingPlans ? (
                  <div className="flex items-center justify-center h-48">
                    <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-accent-primary" />
                  </div>
                ) : plans.length === 0 ? (
                  <div className="text-center py-12 text-txt-tertiary">
                    No training plans yet. Create your first plan to get started.
                  </div>
                ) : (
                  <div className="space-y-3">
                    {plans.map((plan) => (
                      <div
                        key={plan.id}
                        className="p-4 bg-surface-elevated rounded-lg border border-bdr-default
                                   hover:border-accent-primary/50 transition-colors cursor-pointer"
                        onClick={() => setSelectedPlanId(plan.id)}
                      >
                        <div className="flex items-center justify-between">
                          <div>
                            <h4 className="font-medium text-txt-primary">{plan.name}</h4>
                            {plan.description && (
                              <p className="text-sm text-txt-tertiary mt-1 line-clamp-1">
                                {plan.description}
                              </p>
                            )}
                          </div>
                          <div className="flex items-center gap-2">
                            {plan.phase && (
                              <span className="px-2 py-0.5 text-xs font-medium bg-accent-primary/20 text-accent-primary rounded">
                                {plan.phase}
                              </span>
                            )}
                            <button
                              onClick={(e) => {
                                e.stopPropagation();
                                setSelectedPlanId(plan.id);
                                setShowAssignmentModal(true);
                              }}
                              className="px-2 py-1 text-xs font-medium text-accent-primary
                                         hover:bg-accent-primary/10 rounded transition-colors"
                            >
                              Assign
                            </button>
                          </div>
                        </div>
                      </div>
                    ))}
                  </div>
                )}
              </div>

              {/* Quick Stats */}
              <div className="space-y-4">
                <div className="p-4 bg-surface-elevated rounded-lg border border-bdr-default">
                  <h4 className="font-medium text-txt-primary mb-3">Quick Stats</h4>
                  <div className="space-y-2 text-sm">
                    <div className="flex justify-between">
                      <span className="text-txt-secondary">Active Plans</span>
                      <span className="font-medium text-txt-primary">
                        {plans.filter((p) => !p.isTemplate).length}
                      </span>
                    </div>
                    <div className="flex justify-between">
                      <span className="text-txt-secondary">Templates</span>
                      <span className="font-medium text-txt-primary">
                        {plans.filter((p) => p.isTemplate).length}
                      </span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </Tab.Panel>
        </Tab.Panels>
      </Tab.Group>

      {/* Workout Modal */}
      <Dialog
        open={showWorkoutModal}
        onClose={() => {
          setShowWorkoutModal(false);
          setSelectedSlot(null);
        }}
        className="relative z-50"
      >
        <div className="fixed inset-0 bg-black/50" aria-hidden="true" />
        <div className="fixed inset-0 flex items-center justify-center p-4">
          <Dialog.Panel className="w-full max-w-2xl bg-surface-default rounded-lg shadow-xl border border-bdr-default p-6">
            <Dialog.Title className="text-lg font-semibold text-txt-primary mb-4">
              {selectedSlot ? 'Create Workout' : 'Add Workout'}
            </Dialog.Title>
            <WorkoutForm
              planId={selectedPlanId || plans[0]?.id || ''}
              initialDate={selectedSlot?.start}
              onSubmit={handleWorkoutSubmit}
              onCancel={() => {
                setShowWorkoutModal(false);
                setSelectedSlot(null);
              }}
              isSubmitting={isCreating}
            />
          </Dialog.Panel>
        </div>
      </Dialog>

      {/* Assignment Modal */}
      <Dialog
        open={showAssignmentModal}
        onClose={() => setShowAssignmentModal(false)}
        className="relative z-50"
      >
        <div className="fixed inset-0 bg-black/50" aria-hidden="true" />
        <div className="fixed inset-0 flex items-center justify-center p-4">
          <Dialog.Panel className="w-full max-w-2xl bg-surface-default rounded-lg shadow-xl border border-bdr-default p-6">
            <Dialog.Title className="text-lg font-semibold text-txt-primary mb-4">
              Assign Training Plan
            </Dialog.Title>
            <AssignmentManager
              plan={plans.find((p) => p.id === selectedPlanId)}
              onSuccess={() => setShowAssignmentModal(false)}
              onCancel={() => setShowAssignmentModal(false)}
            />
          </Dialog.Panel>
        </div>
      </Dialog>

      {/* Audit Report Modal */}
      <Dialog
        open={showAuditModal}
        onClose={() => setShowAuditModal(false)}
        className="relative z-50"
      >
        <div className="fixed inset-0 bg-black/50" aria-hidden="true" />
        <div className="fixed inset-0 flex items-center justify-center p-4 overflow-y-auto">
          <Dialog.Panel className="w-full max-w-4xl bg-surface-default rounded-lg shadow-xl border border-bdr-default p-6 my-8">
            <NCAAAuditReport
              weekStart={startOfWeek(new Date(), { weekStartsOn: 1 })}
              teamName="Team"
              onClose={() => setShowAuditModal(false)}
            />
          </Dialog.Panel>
        </div>
      </Dialog>
    </div>
  );
}

// Icon components
function CalendarIcon({ className }: { className?: string }) {
  return (
    <svg className={className} fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
    </svg>
  );
}

function ShieldIcon({ className }: { className?: string }) {
  return (
    <svg className={className} fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
    </svg>
  );
}

function ClipboardIcon({ className }: { className?: string }) {
  return (
    <svg className={className} fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" />
    </svg>
  );
}

export default CoachTrainingPage;
```
  </action>
  <verify>
File exists and exports CoachTrainingPage.
  </verify>
  <done>
CoachTrainingPage integrates all training components with tab navigation and modals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add route and navigation</name>
  <files>src/app/routes/index.tsx, src/v2/components/shell/WorkspaceSidebar.tsx</files>
  <action>
Add training route and navigation link.

**1. Update src/app/routes/index.tsx:**
Add route for CoachTrainingPage:
```typescript
// Add import
import { CoachTrainingPage } from '../../v2/pages/CoachTrainingPage';

// Add route (within coach routes section)
{
  path: 'training',
  element: <CoachTrainingPage />,
}
```

**2. Update src/v2/components/shell/WorkspaceSidebar.tsx:**
Add Training link to coach navigation items:
```typescript
// In the coach navigation items array, add:
{
  name: 'Training',
  href: '/app/training',
  icon: CalendarDaysIcon, // or appropriate icon
}
```

Note: The exact locations depend on existing file structure. Follow the pattern of existing routes like seat-racing.
  </action>
  <verify>
1. Navigate to /app/training shows CoachTrainingPage
2. Training link appears in coach sidebar
3. All tabs and modals function correctly
  </verify>
  <done>
Training page accessible via navigation and URL routing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Training Plans & NCAA Compliance feature:
- Training calendar with month/week views
- Drag-drop workout rescheduling
- Periodization timeline (base/build/peak/taper)
- Workout form with exercise management
- Plan assignment to athletes
- NCAA 20-hour rule tracking
- Compliance dashboard with warnings
- Printable audit reports
  </what-built>
  <how-to-verify>
1. Navigate to http://100.86.4.57:3001/app/training
2. Verify Training page loads with three tabs (Calendar, Compliance, Plans)
3. Test Calendar tab:
   - Switch between month and week views
   - Click on a date to open workout creation modal
   - (If workouts exist) Try dragging a workout to reschedule
4. Test Compliance tab:
   - View weekly hours table
   - Check training load chart
   - Click "Generate Audit Report" button
5. Test Plans tab:
   - View list of training plans
   - Click "Assign" on a plan to open assignment modal
6. Verify navigation link in sidebar works
7. Check mobile responsiveness
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 10, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. CoachTrainingPage renders all three tabs
2. Calendar shows workouts and supports drag-drop
3. Compliance dashboard shows NCAA hours and warnings
4. Plans tab lists plans with assignment capability
5. Modals open/close correctly
6. Route works at /app/training
7. Navigation link appears in sidebar
</verification>

<success_criteria>
- Coach can access Training page from navigation
- Training page shows calendar, compliance, and plan management tabs
- All training components render without errors
- Navigation and routing work correctly
- All Phase 10 requirements (TRAIN-01 through TRAIN-10, NCAA-01 through NCAA-04, ATT-04) are complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-training-plans-ncaa-compliance/10-10-SUMMARY.md`
</output>
